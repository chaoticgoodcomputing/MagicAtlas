[project]
name = "magic-atlas-mast"
version = "0.1.0"
description = "Python steps for the MAST test/triage Flowthru project (interaction-graph visualization)."
requires-python = ">=3.11"
dependencies = [
  "networkx>=3.0",
  "pandas>=2.0.0",
  "plotly>=5.20.0",
  "pyarrow>=18.0.0",
]

[tool.uv]
package = false
