{
  "$scaffold": "port-topology target map (ADR-0003 Stage 0a)",
  "$status": "hypothesis — versioned. ADR-0004 §7 (issue #26, 2026-07-20) DELETED the sections that had become a hand-maintained DECLARED half of a generated artifact: stems_representative, aliases, attribute_axes, holes. Measured against the 64-gold rollup, all four were strictly subsumed by witnessed reality (0 declared-but-unwitnessed stems, 12 witnessed-but-never-predicted, axes a subset, all 6 holes witnessed). port-topology.json's stems/attribute_axes are now derived from golds ALONE and it no longer carries aliases/holes at all. What survives here is only the vocabulary NO gold can witness (kinds, supergroups, event_verbs_no_supergroup) plus the two sections awaiting their own issues: connectivity_predictions (→ #27, executable sweeps) and reject (→ #28, asserted-absence golds). ADR-0004 §7 (issue #27, 2026-07-20) then DELETED connectivity_predictions: P1–P6 are executable sweeps over the witnessed graph in Tests/InteractionRollup/ConnectivityPredictionSweeps.cs, each naming what would falsify it. Executing them for the first time found a real false prune in PortFlowMatcher (P1's removal:creature hardcode) and a gold-vocabulary defect (P2's nine bare sac-fodder consumes); P6 was re-expressed against the six former holes' now-witnessed stems, since #26 deleted the registry it named. A prediction checked by nothing is not falsifiable, so it does not live in a data file.",
  "$version": 3,
  "$deleted": "Record of the FOUR facts the #26 deletion removed that no gold witnesses — kept as inert provenance prose (a $-key the generator skips), never as live declared data, so nothing is silently destroyed. (1-3) three is-a parent OVERRIDES the naming spine cannot express: removal:creature ⊂ removal:permanent, removal:artifact ⊂ removal:permanent, modification:evasion ⊂ modification:keyword. `parent` in port-topology.json is now purely name-derived (stem up to the last ':'), which is what its schema always documented it to be; no code ever read the overrides. (4) the attribute axis `owner` (lattice 'owned (ownership, orthogonal to control)', licensed_by objects:*/removal:*/deployment:*) — a DECLARED axis that 64 golds have never witnessed, carried in the artifact with empty stems[] and empty values_seen[]. It is the one unfalsified prediction the deletion drops; it returns to the artifact the moment a gold's port carries an owner attr. Full pre-deletion text: `git show HEAD~1 -- tests/magic-ast-tests/Fixtures/Interactions/topology-scaffold.json`. These are is-a/ontology rulings, which ADR-0004 §1 routes to ADR-0003 prose rather than to a generated artifact.",
  "$provenance": "4-panelist blind sweep (3 Opus + 1 full-slang), 2026-07-16; unanimous kinds/supergroups/axes; forks F1/F2/F3 human-ruled — the rulings live in ADR-0003 §4a, not here (they are architectural, not witnessable; a ruling with two homes can disagree with itself).",

  "kinds": {
    "EVENT":    "discrete occurrences with payloads — what subscriptions (triggered abilities) fire on.",
    "STATE":    "pools/stores folded over the event stream (state = fold(events)).",
    "BEHAVIOR": "continuous time-varying values — statics, anthems, keyword grants, 'as long as'."
  },

  "supergroups": {
    "$note": "`stems` lists the member stems whose NAME does not already begin with the supergroup's own name — the only membership fact a gold cannot witness (a gold declares a port's stem, never its supergroup). It moved here from the deleted stems_representative[*].supergroup field (#26): membership belongs with the definition it qualifies, and this section is not itself part of a generated artifact. Prefix-named stems (removal:*, deployment:*, modification:*, structure:*) resolve by name and are deliberately NOT listed.",
    "removal":      { "kind_view": "EVENT",       "def": "a zone-change whose FROM endpoint is play (battlefield/stack): an object leaves play. Carries from/to/manner." },
    "deployment":   { "kind_view": "EVENT",       "def": "a zone-change whose TO endpoint is play: an object enters play (cast/ETB/token/reanimate/blink-return/copy)." },
    "modification": { "kind_view": "BEHAVIOR",    "def": "an object altered in place: P/T, keyword/ability grant, type/color/control change." },
    "resources":    { "kind_view": "STATE",       "def": "fungible pools: mana, cards, life, counters.", "stems": ["mana", "life", "cards", "counters"] },
    "objects":      { "kind_view": "STATE",       "def": "addressable permanents on the battlefield (F2). Objects = fold(deployment − removal). Bare object-consumes (a sac's fodder decrement) live here; the §10 balance substrate.", "stems": ["creature", "artifact"] },
    "structure":   { "kind_view": "EVENT+STATE",  "def": "the clock — a VIEW spanning phase/combat/extra-turn Events and tap/untap availability State (F1: a supergroup may span kinds).", "stems": ["tap"] }
  },

  "event_verbs_no_supergroup": {
    "dice-rolled":     { "kind": "EVENT", "def": "a die roll produces a numeric payload; feeds 'whenever you roll one or more dice' (O19 — dice dissolved, no fourth axis)." },
    "damage-dealt":    { "kind": "EVENT", "def": "damage as an event; edges into resources:life (loss) and, when lethal, removal (state-based death)." },
    "combat-presence": { "kind": "EVENT", "def": "a permanent attacks or blocks (O21; the attacksorblocks subscription; emit:additionalcombat re-raises the phase that produces it)." }
  },

  "reject": {
    "$note": "NOT AST-derivable — a game-plan/quality/social judgment, never a port. (A rejected archetype's MECHANISMS still map; the LABEL does not.)",
    "archetypes": ["aggro", "control", "combo", "midrange", "tempo", "aristocrats", "voltron", "superfriends", "pillowfort", "group-hug", "stax"],
    "judgments":  ["value", "engine", "payoff", "inevitability", "tech", "gas", "jank", "pile", "goodstuff", "bomb", "haymaker", "threat", "answer", "clock", "out", "win-con", "finisher"],
    "social":     ["durdle", "brew", "netdeck", "spike/timmy/johnny", "lucksack", "whale", "misplay"]
  }
}
