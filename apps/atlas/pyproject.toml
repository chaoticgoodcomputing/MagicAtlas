[project]
name = "magic-atlas"
version = "0.1.0"
description = "Python steps for the MagicAtlas Flowthru pipeline (BERT embedding + UMAP reduction)"
requires-python = ">=3.11"
dependencies = [
  "pandas>=2.0.0",
  "pyarrow>=18.0.0",
  "sentence-transformers>=3.0.0",
  "umap-learn>=0.5.5",
]

[tool.uv]
package = false
