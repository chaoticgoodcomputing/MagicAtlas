# MAST judge — DELTA verdict (slice PB-3)

**Date:** 2026-06-16
**Slice:** PB-3 — structured-characteristic megaslice (structured-characteristic axis + comparative-power merge)
**Scope:** 1 regenerated gold (delta judgment, working tree)
**Result:** PASS

## Summary

- PASS: 1
- FAIL: 0

## Delta scope

PB-3 owns the structured-characteristic axis. On this gold the target residual was the
`noncreature` type-negation, previously encoded as a free-text escape hatch
(`Characteristics: [{CharacteristicType: "other", Description: "noncreature"}]`).

## PASS verdicts

- `tests/magic-ast-tests/Fixtures/HandParsedCards/ZEN/SpellPierce.json` — PASS.
  Real card: "Counter target noncreature spell unless its controller pays {2}." (confirmed
  against oracle-cards.json / Scryfall; matches Input.OracleText).
  - (a) Target structured correctly: `noncreature` → `ExcludedCardTypes: ["creature"]`, the
    established negation axis (ObjectFilter.cs:34, used in 16 golds), faithful to the card and
    named explicitly in the slice's reuse-list. Card type per CR 205.2a; Counter action per CR 701.6.
  - (b) No new free-text/unparsed residual: the prior `other`/`Description` escape hatch is gone;
    the only remaining `Raw` fields (`TypeLine.Raw`, `manaCost.Raw`) are verbatim-by-design exempt.
  - (c) No regression: co-occurring `CardTypes: ["spell"]` preserved; `counterSpell` effect,
    `preventable`/`Unless {Controller pays {2}}` wrapper, target reference, and all Attributes
    serialize unchanged. Removal of null `Power`/`Toughness` from `Input` is correct (Spell Pierce
    is an instant). Removed from whitelist-freetext.json (confirmed absent).

## Out-of-scope residual remaining

None on this gold — fully cleaned in one regeneration. No S6-shared other/another exclusion
residual applies to Spell Pierce.

## Process notes

- Rules data resolved at `libs/mtg-rules/Data/_03_Primary/Datasets/rules-structure.json` (the
  SKILL's `tests/atlas-flow-test/...` path is absent in this checkout); CR 701.6 (Counter) and
  CR 205.2a (card types) both present.
- No new discriminator introduced on this gold (reuses the existing `ExcludedCardTypes` axis), so
  no PortWalk projection decision is in scope for this target.

ALL PASS
