# MAST judge — batch-3 (2026-07-10) SHARD A verdict

**Date:** 2026-07-10
**Base sha:** `22950ec2`
**Scope:** 4 branches (new-node families) — 18 judged targets (4 fixtures, 8 AST nodes, 4 static/triggered rules, 4 projection decisions; some folded)
**Result:** FAIL

## Summary

- PASS: 16
- FAIL: 2
- HALT branch: `mast-tdd/2026-07-10-self-subtype-add`

## FAIL verdicts

### libs/magic-ast/AST/Effects/CardFlow/RevealTopMayPutSharesCreatureTypeRestToBottomEffect.cs
**Verdict:** FAIL
**Issue:** Wrong / contradictory CR citation — cites **CR 400.4** for the "random-order bottom-of-library remainder."
**Rule citation:** CR 400.4 (as it exists in `rules-structure.json`)
**Rule text:** > "Cards with certain card types can't enter certain zones."
**What the AST says:** doc-comment: `"CR 400.4: cards placed on the bottom of a library 'in a random order' are shuffled among themselves before being placed."`
**Why this misrepresents the rule:** CR 400.4 in the rules data is about card-type/zone restrictions — an entirely different concept from library ordering. No rule in subsections 400 or 401 covers "random order" placement (401.4 is *owner-arranged any order*; 103.3/903.6/702.75a are the only "random order" texts, none general). This matches the doctrine's contradiction example (citing an unrelated rule). The other two citations on this node — CR 701.20 (Reveal) and CR 205.3m (creature types) — are correct; the AST shape itself is faithful.
**Suggested fix:** Drop the CR 400.4 citation, or replace it. The sibling `RevealTopPutMatchingToHandEffect` cites **CR 401.4** for library remainder ordering; note that 401.4 is "any order," not "random order," so the cleanest fix is to remove the numeric cite and describe the random-order behavior as a self-contained instruction (Hideaway CR 702.75a is the only keyword precedent for "on the bottom ... in a random order").

### libs/magic-ast/Parsing/Parsers/Triggered/Rules/RevealTopMayPutSharesCreatureTypeRestToBottomTriggeredRule.cs
**Verdict:** FAIL
**Issue:** Same wrong CR 400.4 citation.
**What the AST says:** doc-comment: `"CR 701.20 (Reveal); CR 205.3m (creature types); CR 400.4 (the remainder placed on the bottom in a random order)."`
**Why this misrepresents the rule:** Identical to above — 400.4 is a card-type/zone rule, unrelated to random-order library placement. Parser behavior and emitted AST are correct; the defect is citation-only.
**Suggested fix:** Remove/replace the 400.4 citation in this doc-comment (and in the `known-coarse-projections.json#revealTopMayPutSharesCreatureTypeRestToBottom` reason string, which carries the same wrong cite).

## PASS verdicts

**base-pt-set (H14)**
- `SetBasePTEffect.cs` — PASS. Layer-7b base-P/T **SET** on `EnchantedOrEquipped`; CR **613.4b** ("Effects that refer to the base power and/or toughness of a creature apply in this layer") matches exactly, and the node is correctly distinguished from ModifyPT (7c) and DefinePT (7a).
- `SetBasePTRule.cs` — PASS. Tightly anchored `^(Enchanted|Equipped) creature has base power and toughness N/M.$`; emits `SetBasePTEffect(EnchantedOrEquipped, literal, literal)`.
- `ReduceInStature.json` — PASS. `enchantRestriction(creature)` + `setBasePT(0/2, EnchantedOrEquipped)`; no unparsed, no free text.
- **Projection `setBasePT -> set:pt` (PortGraph + PortWalkProjection)** — PASS. Genuinely **inert**: an Emit-only `set:pt` port with no matching Consume port anywhere in `mast-interaction` (mirrors `modify:pt`/`switch:pt`; ADR 0002 states inert emits have no outgoing edge and are never cycle members). Fabricates **no** cross-card edge; bench:recall stays neutral. The projection could not create an edge — nothing to flag.

**damage-cant-be-prevented (H15)**
- `CantPreventDamageEffect.cs` — PASS. Empty global-lock marker with no player/source/recipient scope (the oracle text names none); CR **615.1** (prevention effects) matches; mirrors `CantBeCounteredEffect`'s marker-only shape.
- `CantPreventDamageRule.cs` — PASS. Anchored `^Damage can't be prevented.$`.
- `LeylineOfPunishment.json` — PASS. `optional(openingHand)` + `cantGainLife(EachPlayer)` + `cantPreventDamage`; no unparsed, no free text.
- **Projection `cantPreventDamage` (coarse)** — PASS. Coarse is sensible: a rules-of-the-game prohibition that emits/consumes no resource; sibling of `cantGainLife`/`cantBeCast` coarse statics; no flow rule reads prevention-lock (base `preventDamage` is itself coarse).

**hands-revealed (H17)**
- `PlayWithHandsRevealedEffect.cs` — PASS. `Player`-scoped (`EachPlayer`/`EachOpponent`) continuous static; CR **701.20a** exists and matches (its own worked example is the Telepathy phrasing). Revelation = `EachPlayer` is correct.
- `PlayWithHandsRevealedRule.cs` — PASS. **No collision.** Base `22950ec2` has no prior Telepathy / "hands revealed" / `handsRevealed` handler (codebase grep empty), so the new opponents-variant is the first and only handler — nothing is double-matched or shadowed. Both patterns are anchored `^…$`, isolating the standalone clause; they correctly cover Telepathy, Squee, Seer's Vision, Wisedrafter's Will, and Zur's Weirding's first sentence, and correctly **skip** the "all players" conditional (Urza's Contact Lenses) which starts with "As long as …".
- `Revelation.json` — PASS. `playWithHandsRevealed(EachPlayer)`; no unparsed, no free text.
- **Projection `playWithHandsRevealed` (coarse)** — PASS. Sensible: continuous public-information effect, no resource flow; sibling of `playWithTopRevealed`; no flow rule reads hand-visibility grants.

**self-subtype-add (H05)**
- `SelfIsAlsoSubtypesRule.cs` — PASS (with note). Additive "is also a" grant → `AddTypeEffect(Self, AddedSubtypes)`; `Self` (not literal name) is correct per CR 201.5, subtypes validated against CR 205.3m. The doc-comment quotes CR 205.1b's retention text but labels it "205.1a" — 205.1a is actually the *replace* rule; the additive-retention concept lives at **205.1b**. This is a subrule-letter slip only (the concept is grounded in the adjacent sibling and the quoted text is correct), so per doctrine it is **not** a FAIL. Recommend correcting the letter `205.1a → 205.1b`.
- `ObjectFilter.SharesCreatureTypeWith` axis (+ `ObjectFilterRelations.cs`, `OperatorPropertyTests.IsRelational`) — PASS. Complete, exact mirror of `SharesColorWith`: added in both `UndecidedAxis` and `SupUndecidedAxis` (conservatively treated as undecided, so `Subsumes`/`Intersects` never fabricate a subsumption claim), and registered in `IsRelational`. Sound in isolation (H07's independent copy will dedup at merge).
- `TajuruParagon.json` — PASS. `addType(Self, [Cleric,Rogue,Warrior,Wizard])` + Kicker `additionalCastCost` + `triggered(Enters, IsSelf)` with `InterveningIf keywordCostPaid(Kicker)` + `revealTopMayPutSharesCreatureTypeRestToBottom(You, 6, SharesCreatureTypeWith:Self)`; no unparsed, no free text (fixtures carry no CR citations, so the 400.4 defect does not touch the fixture).
- **Projection `revealTopMayPutSharesCreatureTypeRestToBottom` (coarse)** — PASS on decision-sensibility (library-visibility action, inert, sibling of `revealTopMayPutMatchingRestToGraveyard`/`revealTopPutMatchingToHand`). NOTE: the entry's reason string carries the same wrong "CR 400.4" cite that must be corrected alongside the two node files.

## Glossary gaps

None. All mechanics (base P/T set, damage-prevention lock, play-with-hands-revealed, additive creature subtype, shares-creature-type) map to existing CR rules and glossary concepts.

## Process notes

- The two FAILs are the **same defect** (wrong CR 400.4 citation) appearing in three files: the effect node, the triggered rule, and the coarse-projection reason string. It is a citation-only error — the AST shape, filter, and projection decision for the Tajuru Paragon reveal are all faithful. A single search-and-replace fix on the 400.4 references clears the HALT.
- Consistency of the citation line: 205.1a→205.1b is treated as PASS (adjacent sibling subrule, concept present in data, quoted text correct — doctrine's "subrule-letter imprecision" carve-out), whereas 400.4 is treated as FAIL (cross-subsection number whose actual text is a wholly unrelated card-type/zone rule, matching the doctrine's contradiction example — and no rule in 400/401 covers random-order placement).
- The base-pt-set semantic projection was specifically stress-tested for edge fabrication: it adds only an Emit port (`set:pt`) and no Consume port; no rule in `mast-interaction` produces a `set:pt`/`modify:pt`/`switch:pt` Consume side, so no bipartite edge can form. Inert as claimed.
- No pre-existing Telepathy handler exists on base — the hands-revealed collision concern is unfounded (verified clear).

**HALT: mast-tdd/2026-07-10-self-subtype-add**
