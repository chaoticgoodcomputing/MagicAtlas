# MAST judge — slice verdict: PB-6 — DisplacerKitten

**Date:** 2026-06-16
**Scope:** 1 regenerated gold (delta judgment), DisplacerKitten
**Result:** PASS

## Summary

- PASS: 1
- FAIL: 0

## Slice target

Two coupled defects on DisplacerKitten:
1. "noncreature spell" left an `OtherCharacteristic{"noncreature"}` free-text residual in the spell-cast trigger filter.
2. `AbilityWord:"Avoidance"` falsely asserted a CR 207.2c ability word.

## Delta verdict

### tests/magic-ast-tests/Fixtures/HandParsedCards/DisplacerKitten.json
**Verdict:** PASS

- **Target 1 (noncreature filter) — structured correctly.** Trigger filter is now
  `{CardTypes:["spell"], ExcludedCardTypes:["creature"], Controller:"You"}`, exactly the spec and
  matching the real oracle text ("Whenever you cast a noncreature spell"). Reuses the existing
  non-type negation path already present on this gold's exile target (`ExcludedCardTypes:["land"]`).
  The free-text `Characteristics:[{CharacteristicType:"other", Description:"noncreature"}]` is gone.
- **Target 2 (false ability word) — corrected.** The `AbilityWord:"Avoidance"` field is dropped.
  Per CR 207.2c / glossary, an ability word is an italicized word with no rules meaning that ties
  together similar abilities across *different* cards; "Avoidance" is the card's one-off printed
  italic label, so encoding it as a CR ability word asserted a false concept. Dropping it is the
  correct resolution. "Avoidance" survives only in the verbatim-exempt `OracleText`/`RawText`.
- **No new residual (primary criterion).** Full structural scan of `Output` finds no
  `OtherCharacteristic`, no `"other"` characteristic, no `unparsed`, no leftover `Characteristics`.
- **No regression.** `Kind:triggered`, `Event:SpellCast`, the composite `exile` +
  `returnToBattlefield` effects, the `up to one target nonland permanent you control` filter, and
  `UnderControl:{Owner}` are all preserved. Remaining diff is canonical regeneration noise
  (key reordering, `IsVariable:false`, `Tapped:false`) — no semantic change.

## Shared producer / sibling check (no regression)

- The `noncreature → ExcludedCardTypes:["creature"]` transform propagated identically to
  `WAR/SpellgorgerWeird` and `OTJ/SlickshotShowOff` (verified in their working-tree diffs) — same
  producer method, consistent with folding into PB-3's mapping.
- All three plus `DisplacerKitten` were removed from `whitelist-freetext.json` (the bonus cleanup).
- `ZEN/SpellPierce` is not in this set and remains whitelisted — acceptable; it is other slices' debt.

## Out-of-scope residuals remaining

None on this gold. The slice fully reached its target axis.

## Process notes

`rules-structure.json` / `glossary.json` now live under `libs/mtg-rules/Data/_03_Primary/Datasets/`
(not the path in the SKILL table). Cross-reference performed there. CR 207.2: "The text box may
also contain italicized text that has no game function." Glossary "Ability Word": "An italicized
word with no rules meaning that ties together abilities on different cards... See rule 207.2c."

ALL PASS
