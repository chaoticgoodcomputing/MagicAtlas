# MAST judge — slice verdict (DELTA)

**Date:** 2026-06-16
**Slice:** PB-3 — structured-characteristic megaslice
**Gold judged:** CMD/Ruination (uncommitted working-tree regen)
**Mode:** delta (judge only the change this slice made; out-of-scope residuals are other slices' debt)
**Result:** PASS

## Summary

- PASS: 1
- FAIL: 0

## PASS verdicts

- `tests/magic-ast-tests/Fixtures/HandParsedCards/CMD/Ruination.json` — PASS.
  Slice structured the structured-characteristic axis: the free-text residual
  `Characteristics: [{ "CharacteristicType": "other", "Description": "nonbasic" }]`
  on the destroy target's filter was replaced by the structured axis
  `CardTypes: ["land"]` + `ExcludedSupertypes: ["Basic"]`. CR 205.4c: "Any land
  with the supertype 'basic' is a basic land. Any land that doesn't have this
  supertype is a nonbasic land" — so "nonbasic land" is exactly land-type minus
  the Basic supertype. Faithful to the real card (oracle: "Destroy all nonbasic
  lands.").

## Delta criteria

- (a) TARGET structured correctly: YES — nonbasic free-text → ExcludedSupertypes:[Basic], grounded in CR 205.4a/c.
- (b) No NEW free-text/unparsed beyond scope: YES — gold carries zero free-text; only "nonbasic" occurrence is the exempt verbatim RawText field; no unparsed/Description/Raw* residual introduced.
- (c) No regression: YES — destroy effect, Each target kind, and land card type all preserved; no ability dropped/added/inverted; CMD/Ruination correctly removed from whitelist-freetext.json (gold fully cleaned, not an S6-shared gold).

## Out-of-scope residuals remaining

None on this gold. Ruination is not an [S6-SHARED] gold (no other/another-exclusion
residual), so the slice fully cleaned it and the whitelist entry was removed as
expected.

## Process notes

- rules-structure.json / glossary.json now live under `libs/mtg-rules/Data/_03_Primary/Datasets/`
  (moved from `tests/atlas-flow-test/...` per the SKILL's quick reference). Citations cross-checked
  against the live path.
- GLOSSARY.md cites "Destroy all nonbasic lands." as a canonical ExcludedSupertypes example and
  uses the identical "supertype negated via ExcludedSupertypes" pattern for nonlegendary — the
  slice's axis choice matches established AST convention.
