# MAST judge — DELTA verdict, SLICE PB-4 (Bucket A counter-gate)

**Date:** 2026-06-16
**Scope:** 1 regenerated gold (DELTA judgment, not whole-gold purity) — DKA/StranglerootGeist
**Slice target:** structure the Undying/Persist intervening-if counter-gate via existing ConditionParser.Parse → TriggeringObjectCounterCondition
**Result:** PASS

## Summary

- PASS: 1
- FAIL: 0

## PASS verdicts

- `tests/magic-ast-tests/Fixtures/HandParsedCards/DKA/StranglerootGeist.json` — PASS.
  - (a) TARGET structured correctly: `InterveningIf` moved from `OtherCondition{Text:"it had no +1/+1 counters on it"}` to `TriggeringObjectCounterCondition{ConditionType:"triggeringObjectCounter", CounterType:"+1/+1", Present:false}`. Faithful to the real Scryfall oracle ("Undying (When this creature dies, if it had no +1/+1 counters on it, ...)"), which equals CR 702.93a verbatim. Undying ⇒ +1/+1, Present:false correctly encodes "had no counters."
  - (b) No new free-text/unparsed residual introduced. The slice's sink (OtherCondition) is removed, not relocated. Card removed from `whitelist-freetext.json`.
  - (c) No regression: Trigger{Timing:When, Event:Dies, Filter{CardTypes:[creature], IsSelf:true}}, returnToBattlefield{Target:It, UnderControl:Owner, WithCounters:+1/+1 x1}, KeywordSource:"Undying"/"Haste", and Reminder all preserved. Remaining diff hunks are pure canonical reserialization: `Tapped:false` (real serializer field on returnToBattlefield, see DrownyardTemple), `IsVariable:false` on manaCost, JSON key reordering, and `—`→`—` unescaping. No ability dropped/added/inverted; sibling effects intact.

This gold carries no out-of-scope residual after the change. Haste is a clean keywordAbility; the only condition on the card was the one this slice structured.

## Process notes

ConditionParser already matches both counter texts (Condition.cs L76 doc-comment; "+1/+1" and "-1/-1" arms), so the slice correctly routed the three hardcoded producers (PersistKeyword.cs, UndyingKeyword.cs, TargetCreatureGainsKeywordRule.cs) through the existing Parse path with no new regex arm and no new AST/schema. Node cites CR 603.10 (dies-triggers look back in time) and CR 603.4 (intervening-if) — both present in rules-structure.json and consistent with the modeling.
