# MAST judge — batch-4 (2026-07-10) SHARD C verdict

**Date:** 2026-07-10
**Scope:** 10 targets (5 fixtures, 5 AST/parser files) across grant/anthem/modal reuse families
**Result:** PASS

## Summary

- PASS: 10
- FAIL: 0

## FAIL verdicts

None.

## PASS verdicts

### I01 — chosen-type-grant-controlled (Arcane Adaptation)
- `tests/magic-ast-tests/Fixtures/HandParsedCards/ArcaneAdaptation.json` — PASS. Additive "are the chosen type in addition to their other types" modeled as three sibling `AddTypeEffect` nodes (creatures you control; creature spells you control = CardTypes:["spell","creature"]; creature cards you own = Owner:You) each carrying structured `ChosenSubtypeReference:CreatureType`. Line-1 binder is the reused decomposed `When:asThisEnters` + `chooseCreatureType` (timing NOT baked into the effect discriminator). The chosen-type link is a **structured** reference, not a free-text `"of the chosen type"` string — it is the remedy for the linked-variable anti-pattern, not the anti-pattern. CR 205.1b ("in addition to its other types"), CR 205.3m (creature-type list incl. the chosen type), CR 607.1 (linked abilities).
- `libs/magic-ast/Parsing/Parsers/Static/Rules/CreaturesAreChosenTypeGrantRule.cs` — PASS. Anchored full-paragraph regex; mirrors `CreaturesAreEveryCreatureTypeRule` (Maskwood) and reuses batch-3 `AddTypeEffect`+`ChosenSubtypeReference`. Disjoint from the every-type sibling by wording. (Doc-comment attributes the additive-retention quote to "205.1a"; the verbatim text is actually **205.1b** — subrule-letter imprecision only, not a FAIL per doctrine.)

### I04 — grant-activated-ability-controlled (Manaweft Sliver)
- `tests/magic-ast-tests/Fixtures/HandParsedCards/ManaweftSliver.json` — PASS. The granted `"{T}: Add one mana of any color."` is a fully **structured** activated mana ability (Costs:[tap], addMana AnyColor:true, IsManaAbility:true) nested in a `GainAbilityEffect` over creature+Sliver+Controller:You — no free-text body. CR 605.1a (mana-ability criteria), CR 611.1 (continuous effect).
- `libs/magic-ast/Parsing/Parsers/Static/Rules/SubtypeCreaturesHaveQuotedAbilityRule.cs` — PASS. Quoted body dispatched to `ActivatedAbilityParser` (Triggered fallback); returns null on unrecognized body (honest gap). Anchored. CR 113.3 is a loose paraphrase in the doc-comment but is topically correct (113.3b defines activated abilities) and non-contradictory.

### I07 — token-keyword-pair-grant (Aven Wind Guide)
- `tests/magic-ast-tests/Fixtures/HandParsedCards/AvenWindGuide.json` — PASS. "Creature tokens you control have flying and vigilance" → two `GainAbilityEffect` over creature+IsToken:true+Controller:You (flying→evasion, vigilance→keywordAbility), matching the card's own line-1 keyword modeling. Line-1 (Flying, vigilance) and line-3 (Embalm activated ability) both parse structured; no unparsed nodes. CR 702.9 (Flying), CR 702.21 (Vigilance), CR 604.2 (static continuous effect).
- `libs/magic-ast/Parsing/Parsers/Static/Rules/CreatureTokensHaveKeywordListRule.cs` — PASS. New sibling; batch-1's `ControlledFilterHaveKeywordListRule` was NOT edited (confirmed via merge diff). Mandatory `" and "` conjunction keeps it disjoint from `BareKeywordGrantRule` Arm 2's single-keyword shape.

### I09 — attacking-subtype-anthem-keyword (Blight Mound)
- `tests/magic-ast-tests/Fixtures/HandParsedCards/BlightMound.json` — PASS. "Attacking Pests you control get +1/+0 and have menace" → one `StaticAbility` with `ModifyPTEffect`(+1/+0) + `GainAbilityEffect`(menace, MinimumBlockers:2) sharing a creature+Pest+Controller:You+`CombatStateCharacteristic`:Attacking filter. Line-2 dies-trigger (nontoken creature you control dies → create 1/1 B/G Pest with nested "When this dies, gain 1 life") parses structured; no unparsed. CR 508 (declare-attackers / attacking state), CR 611.3 (static continuous P/T effect), CR 702.111 (Menace, MinimumBlockers:2).
- `libs/magic-ast/Parsing/Parsers/Static/Rules/AttackingSubtypeAnthemAndKeywordGrantRule.cs` — PASS. Mirrors `TribalAnthemAndKeywordGrantRule` with a mandatory "Attacking" prefix → disjoint from the Tribal/Supertype anthem siblings (which were not edited). Reuses `ModifyPTEffect`/`GainAbilityEffect`/`CombatStateCharacteristic`. CR 109.5 ("you control" = source controller).

### I20 — modal-choose-one (Goblin War Party)
- `tests/magic-ast-tests/Fixtures/HandParsedCards/GoblinWarParty.json` — PASS. "Choose one" modeled with the pre-existing structured `ModalAbility` (ModeSelection Min/Max 1/1, 2 Modes, AllowDuplicates:false); mode 1 = createToken ×3 1/1 red Goblin; mode 2 = `CompositeEffect`(modifyPT +1/+1 EOT + gainAbility haste EOT). Entwine modeled as a structured static optional `additionalCastCost` {2}{R}. Each mode's inner effect is structured — no free text. CR 700.2 (modal), CR 702.42 (Entwine), CR 113.3a (spell ability).
- `libs/magic-ast/Parsing/Parsers/Spell/Rules/MassAnthemAndGainKeywordSpellRule.cs` — PASS. Emits a single `CompositeEffect`(ModifyPTEffect + one GainAbilityEffect per keyword) sharing duration+target. The added `AbilityClassifier` route is anchored on the "and gain <kw> until end of turn" tail and placed after the P/T-only block, so it does not shadow `MassAnthemSpellRule` nor reroute pure-anthem siblings (which lack the "and gain … until end of turn" tail). CR 113.3a (one-shot imperative spell effect), CR 611 (simultaneous continuous effects).

## Projection decisions (initiative 03)

None required. Shard C is a **reuse-only** shard: cross-checked the discriminator baseline diff (`89a0ac4e`) — the only addition after batch-4 was `OracleEffect:cantDrawCards`, which belongs to a *different* shard's family (Maralen / players-cant-draw), not to any of these five. All five families reuse pre-existing discriminators (`AddTypeEffect`+`ChosenSubtypeReference`, `GainAbilityEffect`, `ModifyPTEffect`, `CombatStateCharacteristic`, `ModalAbility`/`ModeSelection`/`CompositeEffect`, `additionalCastCost`), so no new `PortGraph`/`PortWalkProjection` case or `known-coarse-projections.json` entry was needed and the exhaustiveness ratchet had nothing to enforce.

## Glossary gaps

None surfaced.

## Process notes

- **Citation cross-references (all pass):** verified 607.1, 113.3, 611.1, 604.2, 702.9, 702.21, 702.111, 611.3, 109.5, 700.2, 205.3m, 113.3a, 605.1a, 508 all present in `rules-structure.json`. Two loose spots, both under the doctrine's "don't nitpick / FAIL only on absent-or-contradictory" bar and neither a FAIL:
  1. `CreaturesAreChosenTypeGrantRule` doc-comment labels the "in addition to its other types" additive-retention quote as **205.1a**; that verbatim text is actually **205.1b** (205.1a is the type-*setting*/replacement subrule). Subrule-letter imprecision only.
  2. `SubtypeCreaturesHaveQuotedAbilityRule` doc-comment paraphrases **113.3** as "Some effects and static abilities can grant an object an activated ability." 113.3's literal text is the four-category ability taxonomy (113.3b defines activated abilities). Topically correct and non-contradictory; the modeling is additionally grounded by the correct CR 611.1 cite.
- **Out-of-scope pre-existing bug (noted, not judged):** the I04 worker flagged a 4-quote escaping dead-code bug in `EquippedCreatureHasQuotedAbilityRule`. That is not this branch's file and is not a defect of the judged `SubtypeCreaturesHaveQuotedAbilityRule` — surfaced for the orchestrator's backlog, not a FAIL here.
- **Isolation confirmed:** each merge touched only its own new rule file (+ fixture); family 5 additionally added the `AbilityClassifier` route only. No sibling anthem/grant rule was edited, so disjointness claims hold structurally.
- **No unparsed nodes** in any of the five gold fixtures.

## Result

ALL PASS
