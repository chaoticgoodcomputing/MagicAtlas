# MAST judge — batch verdict (2026-07-10 b3-B)

**Date:** 2026-07-10
**Shard:** batch-3 SHARD B — new-discriminator / axis / shared-field families
**Base sha:** 22950ec2
**Scope:** 4 branches — 4 fixtures + 8 AST-node/projection targets (12 items)
**Result:** PASS

## Summary

- PASS: 12
- FAIL: 0

## FAIL verdicts

None.

## Per-branch verdicts

### 1. equipment-unattach-sacrifice (H06) — CR 701.3

- `tests/.../StitchersGraft.json` — **PASS.** modifyPT +3/+3; `doesntUntap{It}` with `WhoseUntapStep:"its controller's next"`; `BecomesUnattached` trigger (Filter=permanent) + `sacrifice{ThatPermanent}`; equip {2}. Oracle byte-matches corpus.
- `TriggerCondition.cs#BecomesUnattached` — **PASS.** New trigger-event discriminator; doc quotes CR 701.3d verbatim (confirmed against rules-structure.json 701.3d). Condition rule regex `\bbecomes unattached from (a|an|the) X\b` correctly matches all four unattach cards (Stitcher's Graft, Grafted Wargear, Grafted Exoskeleton, Captain's Hook), Filter=permanent — correct bonus coverage.
- `ObjectReference.cs#ThatPermanent` — **PASS.** New back-ref kind; correctly kept distinct from `ThatCreature` because CR 701.3d/301.5 type the detached-from object generically as a permanent. The `SacrificeThatPermanentTriggeredRule` is anchored (`^sacrifice that permanent$`), so Captain's Hook's "destroy that permanent" is NOT stolen by the sacrifice rule (verified against corpus).
- `known-coarse-projections.json#triggerEvent.BecomesUnattached` — **PASS (projection).** New discriminator's projection decision is present and sensible: coarse, consistent with the entire sibling `Becomes*` family (Blocked/Crewed/Tapped/Target/Untapped all coarse). The downstream sacrifice/destroy payoff is captured by the separately-projected `SacrificeEffect`, so the unattach event itself is not something a flow rule would clearly want. `ItDoesntUntapTriggeredRule` anchored, maps "it" → `It` (trigger subject) correctly.

### 2. choose-creature-type (H07) — CR 614.12 / 205.3

- `tests/.../TitanOfLittjara.json` — **PASS.** Producer "As this creature enters, choose a creature type" → `chooseCreatureType` + `When:asThisEnters` — the correct timing-composable decomposition, NOT the `chooseCreatureTypeOnEntry` timing-baked anti-pattern. Consumer "is the chosen type in addition to its other types" → `addType{ChosenSubtypeReference:CreatureType}` — a STRUCTURED linked-ability reference (CR 607.1), which is exactly the SKILL's prescribed fix for the `Characteristics:["of the chosen type"]` free-text anti-pattern, not the shortcut. Draw line → optional `drawCards` (count over `SharesCreatureTypeWith:Self` filter) + `IfYouDo` discard (CR 118.12). Oracle byte-matches corpus.
- `AddTypeEffect.cs#ChosenSubtypeReference` — **PASS.** Faithful additive-case parallel of the pre-existing `ChangeSubtypeEffect.ChosenSubtypeReference` (SET sibling) and `ObjectFilter.ChosenCharacteristic` (filter sibling), both present at base — this extends an established structured pattern, does not invent a shortcut.
- `ObjectFilter.cs#SharesCreatureTypeWith` — **PASS.** New relational axis paralleling `SharesColorWith`; correctly distinguished from `ChosenCharacteristic` (live multi-type set vs single fixed chosen value). Registered in both `ObjectFilterRelations` undecided-axis selectors and the operator property test. New draw rule regex is fully anchored to the exact Titan line, so it does not over-match the other "shares a creature type with it" cards (Heirloom Blade / Alpha Status / Stoneforge Masterwork / Shared Animosity / Tajuru Paragon are all pump/reveal, not draw). No new effect/trigger/restriction discriminator → no projection entry required.

### 3. legend-rule-doesnt-apply (H02) — CR 704.5j

- `tests/.../CouncilOfReeds.json` — **PASS.** `legendRuleSuppression{Target:Each creature you control}` scoped variant (CR 704.5j); intervening-if `count` condition on cast-history (`spell`, excluded `creature`, You, `castThisTurn`, `>=1`) per CR 603.4; `copy{Self}` self-by-name token matches the `CreateCopyOnCombatDamage` sibling convention (CR 201.5 / 707). Oracle byte-matches corpus.
- `LegendRuleSuppressionEffect.cs#Target` — **PASS.** Additive optional `Target` (`WhenWritingNull`); Mirror Gallery's unscoped gold is byte-preserved (`{"EffectType":"legendRuleSuppression"}`, fixture untouched). Rule anchored `^...$`.
- `ConditionParser.cs#CastSpellThisTurn` — **PASS.** Anchored (`^you've/have cast a (non-X)? spell this turn$`) — correct bonus coverage across ~14 cards (H.E.R.B.I.E., Invisible Woman, Fortune Teller's Talent, Tapestry, Seeker, Silburlind Snapper, …) and does NOT swallow Hurkyl's distinct "noncreature spells you've cast this turn" counting phrase. No collision with sibling cast-history conditions.

### 4. cant-cast-creature-spells (H11) — CR 601.3a

- `tests/.../SteelGolem.json` — **PASS.** `cantBeCast{Caster:You}` + `AffectedObjects{CardTypes:["spell","creature"]}` — faithful caster-scoped restriction (CR 601.3a). The positive `["spell","creature"]` convention matches existing fixtures (Nullify's `["spell","creature"]`, Disallow, etc.). Oracle byte-matches corpus.
- `CantBeCastEffect.cs#Caster` — **PASS.** Additive optional `Caster` (`WhenWritingNull`) reuses the `cantBeCast` discriminator; Gaddock Teeg's unscoped gold is untouched (no regression). Rule anchored `^You can't cast (non)?<type> spells$`; correct bonus coverage for Nullhide Ferox / Grid Monitor / Nikya, and Ward of Bones ("Each opponent who controls more creatures than you can't cast creature spells") is NOT stolen because its clause does not start with "You".

## Glossary gaps

None surfaced — all mechanics (unattach, legend rule, creature type, cast restriction, chosen type) are covered by rules-structure.json.

## Process notes

- **Projection scope:** only H06 introduces a new discriminator in a projected dimension (`TriggerEvent.BecomesUnattached`). H07's `SharesCreatureTypeWith`/`ChosenSubtypeReference`, H02's optional `Target` + `CastSpellThisTurn` condition, and H11's optional `Caster` are all fields/conditions on pre-existing discriminators, so no new PortWalk projection decision is required for them.
- **H05 dedup:** `SharesCreatureTypeWith` is described as shared with H05 (a different shard). No other `mast-tdd/2026-07-10-*` branch in this shard defines it; this branch's definition (parallel to `SharesColorWith`, `ChosenCharacteristicKind?`… no — `ObjectReference?`) is internally correct. Byte-identity with H05's copy is a merge-time concern the orchestrator must enforce before dedup; it does not affect this branch's per-item fidelity.
- **H07 linked-variable depth:** the producer `chooseCreatureType` still "models only the choice declaration, not the producer/consumer link" (its own doc); the link is by-kind (`ChosenCharacteristicKind.CreatureType`, unique per permanent per CR 205.3). This is the accepted codebase pattern (already used by `ChangeSubtypeEffect`/`ObjectFilter.ChosenCharacteristic` at base), so it is a PASS here; an explicit per-card variable-ID model remains a codebase-wide design item, not a per-item FAIL.

**ALL PASS**
