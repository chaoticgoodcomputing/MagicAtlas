# MAST judge — batch verdict (re-judge)

**Date:** 2026-06-16
**Scope:** 1 file (1 fixture, 0 AST nodes) — re-judge of a previously-FAILed gold now fixed
**Result:** PASS

## Summary

- PASS: 1
- FAIL: 0

## Context

This is a re-judge of `M10/GoblinChieftain` (uncommitted in the working tree). The prior batch
verdict FAILed it: re-pointing the gold to the authoritative text "Other Goblin creatures you
control get +1/+1 and have haste" had produced an `unparsed` node for the "+1/+1 and have haste"
conjunction and lost the `ExcludeSelf` the slice was meant to add. The prescribed remedy
generalized `SupertypeAnthemAndKeywordGrantRule` from supertype-only to also handle
"Other [Subtype] creatures … get +N/+N and have <keyword>" with `ExcludeSelf`.

## FAIL verdicts

(none)

## PASS verdicts

- `tests/magic-ast-tests/Fixtures/HandParsedCards/M10/GoblinChieftain.json` — PASS. Faithful parse
  of the authoritative card (oracle-cards.json confirms type line "Creature — Goblin" and text
  "Haste (…)\nOther Goblin creatures you control get +1/+1 and have haste.").
  - **Ability 1** — static `keywordAbility` Haste, the creature's own keyword, with reminder text
    preserved verbatim (CR 702.10).
  - **Ability 2** — ONE `static` ability carrying TWO effects over the SAME subject filter
    `{CardTypes:[creature], Subtypes:[Goblin], Controller:You, ExcludeSelf:true}`: a `modifyPT`
    +1/+1 AND a `gainAbility` granting Haste. This is the single continuous effect of CR 611.3
    (whose own example is "All white creatures get +1/+1"). The two effect filters are
    byte-identical (verified), so both bonuses land on exactly the Other-Goblins.
  - **Self-exclusion** — the prior free-text `Characteristics:[{CharacteristicType:"other"}]`
    shortcut (a rules-meaningful string, a FAIL anti-pattern) is replaced by the structured
    `ExcludeSelf: true`. "Other Goblin creatures you control" resolves to battlefield permanents
    of that subtype under your control (CR 109.2), minus the source.
  - **No `unparsed` node** anywhere (verified: 0 nodes with Kind/EffectType "unparsed"); no
    dropped, added, or inverted ability; the conjunction is split into the two structured effects.
  - **Off quarantine / whitelist** — not present in `oracle-text-quarantine.json` nor
    `whitelist-unparsed.json`; the gold now stands on its own as eventual-truth.

## Citation cross-reference

- **CR 611.3** — exists; text: "A continuous effect may be generated by the static ability of an
  object. Example: A permanent with the static ability 'All white creatures get +1/+1'…". Matches
  the modeling (one static ability → one continuous effect with both sub-effects). PASS.
- **CR 109.2** — exists; text grounds "a card type or subtype" without a zone word as "a permanent
  of that card type or subtype on the battlefield." Grounds the Goblin-creatures-you-control filter.
  PASS.
- **CR 702.10** — exists ("Haste"); glossary "Haste" present. PASS.

## Projection decision (initiative 03)

Not applicable. The fix introduces no new discriminator — `modifyPT`, `gainAbility`,
`keywordAbility`, and the `ExcludeSelf` filter flag are all pre-existing. No PortWalk projection
decision is required for this re-judge.

## Glossary gaps

(none) — "Haste" is in `glossary.json`.

## Process notes

The working-tree diff confirms the prescribed shape: authoritative oracle text restored (with
reminder text), the free-text `Characteristics:['other']` self-exclusion replaced by structured
`ExcludeSelf:true`, and the previously two-separate-static-abilities (one of which had a filter
missing both `ExcludeSelf` and `CardTypes`) collapsed into one static ability with two effects over
an identical Other-Goblin filter. The earlier FAIL is fully resolved.

ALL PASS
