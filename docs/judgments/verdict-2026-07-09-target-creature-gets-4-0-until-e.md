# MAST judge — batch verdict

**Date:** 2026-07-09
**Scope:** 2 files (1 fixture, 1 parser rule) + 1 projection decision
**Branch:** mast/target-creature-gets-4-0-until-e
**Family:** target-creature-gets-4-0-until-e (Chilling Trap)
**Result:** PASS

## Summary

- PASS: 3
- FAIL: 0

## FAIL verdicts

(none)

## PASS verdicts

- `tests/magic-ast-tests/Fixtures/HandParsedCards/ONS/ChillingTrap.json` — PASS. Input.OracleText
  "Target creature gets -4/-0 until end of turn. If you control a Wizard, draw a card." is
  byte-identical to oracle-cards.json (name/mana `{U}`/type Instant also match). Gold is one `spell`
  ability with two sibling effects: `modifyPT` (Target creature filter, PowerModifier literal -4,
  ToughnessModifier literal 0, Duration `untilTime` {Part: Turn, Edge: End}) and `drawCards`
  (Count 1, Player You, Condition `count` over Filter {Subtypes:[Wizard], Controller:You},
  Comparison GreaterThanOrEqual 1). "-0" toughness modeled as literal 0 is exact (-0 == 0).
  No IUnparsed, no UnstructuredEffect, no OtherCondition, no free-text, no lossy drop/merge. Timing
  lives in a separate composable Duration node, not baked into the effect discriminator. CR 611.2
  (continuous effect generated by a spell's resolution) and CR 121.1 (drawing a card) both apply.
- `libs/magic-ast/Parsing/Parsers/Spell/Rules/ModifyPTThenConditionalDrawRule.cs` — PASS.
  New `[SpellRule]`/`IMultiSpellRule` anchored to the full two-sentence text; sentence 1 must be the
  ModifyPT form, sentence 2 the "If <cond>, draw <count> card(s)" idiom whose condition is routed
  through `ConditionParser` into the structured `DrawCardsEffect.Condition` slot. Emits exactly the
  two-effect gold shape reusing existing nodes (newAstNode=false). Doc-comment cites CR 121.1 and
  CR 611.2 — both present in rules-structure.json with text matching the modeling.
- `mast/target-creature-gets-4-0-until-e#projection` — PASS. No new discriminator introduced
  (newAstNode=false, shared=[]); the shape reuses pre-existing `modifyPT`/`drawCards`/`count`/
  `untilTime` discriminators. No PortWalk projection decision is required; the exhaustiveness ratchet
  is not triggered.

## Glossary gaps

(none)

## Process notes

- CR cross-reference: CR 121.1 confirmed under subsection 121 "Drawing a Card" — text matches the
  doc-comment quote verbatim. CR 611.2 confirmed under subsection 611 "Continuous Effects" — text
  supports "a continuous effect may be generated by the resolution of a spell." Both citations sound.
- All node field shapes verified against source: ModifyPTEffect (Target/PowerModifier/ToughnessModifier),
  DrawCardsEffect (Count/Player/Condition), CountCondition [ConditionKind("count")] (Filter/Count),
  UntilTimeDuration.EndOfTurn ({Part:Turn, Edge:End}), ObjectFilter.Controller (ControllerFilter enum),
  Comparison (Operator ComparisonOperator + Value int?, GreaterThanOrEqual present).

ALL PASS
