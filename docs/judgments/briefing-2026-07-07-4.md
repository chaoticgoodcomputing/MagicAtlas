# MAST TDD batch briefing — 2026-07-07 (battle-test batch 5)

Two clean, mechanical families (both Sonnet — testing the cheap-model policy). Both exemplars
`lossyRisk: false`.

## F1 — life-gain DOUBLING replacement (The Wind Crystal) — SONNET
Target (line 2): `If you would gain life, you gain twice that much life instead.` The DOUBLE variant of
batch 1's "plus N" life-gain replacement — a replacement effect (CR 614.1) that doubles the life-gain
event. Reuse the existing `ReplacementEffect` + `LifeChangeEvent` + `ReplacementModifier` (the modifier
enum already has a `double` value, per batch 1's judge). Lines 1 (`White spells you cast cost {1} less`)
and 3 (`{4}{W}{W}, {T}: Creatures you control gain flying and lifelink…`) parse on this base — author the
whole-card gold faithfully; your work targets ONLY line 2. Verify lines 1+3 aren't lossy before finalizing.
- **CR 614.1** — "Some continuous effects are replacement effects… Such effects watch for a particular event that would happen and completely or partially replace it…"
- Input: `The Wind Crystal` · `{2}{W}{W}` · `Legendary Artifact` · Colors `["W"]` · CI `["W"]`.

## F2 — Ward with a non-mana cost (Sunset Saboteur) — SONNET
Target (line 2): `Ward—Discard a card.` The Ward keyword (CR 702.21) with a NON-mana ward cost (discard a
card) rather than a mana cost. Ward is a triggered ability: whenever this becomes the target of a spell/
ability an opponent controls, counter it unless they pay the ward cost. Reuse the existing Ward keyword
machinery + a discard cost; the novelty is the cost being non-mana. Lines 1 (`Menace`) and 3 (`Whenever
this creature attacks, put a +1/+1 counter on target creature an opponent controls.`) parse on this base.
- **CR 702.21** — Ward (keyword ability). Pull the exact 702.21a text from `rules-structure.json` and cite it verbatim.
- Input: `Sunset Saboteur` · `{1}{B}` · `Creature — Human Rogue` · 4/1 · Colors `["B"]` · CI `["B"]`.
