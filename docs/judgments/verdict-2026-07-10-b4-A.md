# MAST judge — batch verdict (batch-4, 2026-07-10, SHARD A)

**Date:** 2026-07-10
**Scope:** 13 targets across 4 families (4 fixtures, 9 AST nodes/rules/projection) — I11 players-cant-draw, I08 cant-attack-already-attacked, I12 first-spell-each-turn-cost-less, I19 enchant-nonland-restriction
**State judged:** merged HEAD of `feat/loop-trial` (incl. post-merge fix f0c9da5a: I11 draw citation CR 120→121)
**Result:** PASS

## Summary

- PASS: 13
- FAIL: 0

## FAIL verdicts

None.

## PASS verdicts

### I11 — players-cant-draw (Maralen of the Mornsong)
- `tests/.../MaralenOfTheMornsong.json` — PASS. Line-1 `cantDrawCards{Player:EachPlayer}` correctly encodes the symmetric "Players can't draw cards." (CR 604.1 static, CR 611.1 continuous rules-mod, CR 121 the drawing rule it overrides). Line-2 "each player's draw step" → `Trigger{Draw,Beginning,Whose:Any}` + `composite[loseLife 3 ThatPlayer, searchLibrary ThatPlayer Dest:Hand]`. Faithful, anchored, no unparsed/free-text.
- `libs/magic-ast/AST/Effects/CardFlow/CantDrawCardsEffect.cs` — PASS. New draw-lock static mirroring `CantGainLifeEffect`; `Player` scope field kept general for asymmetric variants. **Citation fix confirmed landed**: node doc now reads "CR 121, drawing a card" (120 is Damage) — verified against rules-structure.json (121 = "Drawing a Card", 120 = "Damage").
- `known-coarse-projections.json#cantDrawCards` — PASS (projection decision, initiative 03). The only new effect discriminator in the shard; present as a coarse entry, and the choice is **sensible**: a draw-lock is a rules-of-the-game prohibition (sibling of `cantGainLife`/`cantBeCast`), not an emit/consume flow event — no flow rule would want it. Citation fix 120→121 also landed in this entry.

### I08 — cant-attack-already-attacked (Port Razer)
- `tests/.../PortRazer.json` — PASS. Line-1 folds `untap-each` + `additionalCombatPhase` onto one `DealsCombatDamageToPlayer` trigger; line-2 → `cantAttack{AlreadyAttackedThisTurn:true}` with `Target` null (reflexive "this creature"). Faithful to CR 508.1.
- `CantAttackEffect.cs#AlreadyAttackedThisTurn` — PASS. Additive bool on the existing `cantAttack` discriminator de-strings the "already attacked this turn" idiom exactly as `Alone`/`OnlyAlone` do; CR 508.1c attacker-restriction; reflexive pronoun correctly leaves `Target` null.
- `UntapEachTriggeredRule.cs` — PASS. Reuses `UntapEffect{Kind:Each}`; anchored `^untap each …$` is disjoint from `UntapAllTriggeredRule`'s `^untap all …$`, so it cannot alter behaviour for "untap all" siblings. CR 701.26. (Sibling scan: only Port Razer + Bloodthirster carry the already-attacked line; both parse to the same shape.)

### I12 — first-spell-each-turn-cost-less (Shadow in the Warp)
- `tests/.../ShadowInTheWarp.json` — PASS. Line-1 → `costReduction 2` + `AffectedObjects{[spell,creature],You,History:castThisTurn{Ordinal:1}}` ("first creature spell you cast each turn costs {2} less", CR 601.2f). Line-2 → `SpellCast{spell,ExcludedCardTypes:[creature],Opponent} Ordinal 1 PerTurn` + `dealDamage 2 ThatPlayer`. Faithful.
- `HistoryPredicate.cs#CastThisTurnPredicate.Ordinal` — PASS. Additive `Ordinal` on the existing `castThisTurn` predicate records which occurrence ("first") the oracle names — descriptive-only, reuses `CostReductionEffect`; no new discriminator.
- `CastsNthSpellEachTurnConditionRule.cs` — PASS. Priority 999 (above generic `SpellCastConditionRule` 998); `Match` requires both "each turn" AND a whitelisted ordinal, so generic "whenever you cast a spell" siblings (no each-turn/ordinal) fall through — no over-capture.

### I19 — enchant-nonland-restriction (Detention Vortex)
- `tests/.../DetentionVortex.json` — PASS. "Enchant nonland permanent" → `enchantRestriction{CardTypes:[permanent],ExcludedCardTypes:[land]}` (CR 702.5a). The compound "can't attack or block, and its activated abilities can't be activated" is **fully structured** into `cantAttack`/`cantBlock`/`cantActivateAbilities` (all `EnchantedOrEquipped`). Activated `{3}` self-destroy carries `WhoMayActivate:Opponent` + `OnlyAsSorcery`. No free-text.
- `EnchantRule.cs#nonTypeBranch` — PASS. Widened shared rule's `non<type> <type>` branch routes "nonland permanent" to `CardTypes:[permanent]+ExcludedCardTypes:[land]` (CR 109 type negation, CR 702.5a), parallel to the codebase's nonland-card convention — no `Characteristics:["nonland permanent"]` free-text.
- `ActivatedAbilityParser.cs#ExtractActivationPermission` — PASS. Additive `ActivationPermission.Opponent` (CR 602.2 "unless the object specifically says otherwise"); anchored `^only your opponents may activate…$` matches the full sentence only and folds "and only as a sorcery" → `OnlyAsSorcery`. Sibling scan (Knight of the Holy Nimbus, Clergy, Soul Ransom, Oft-Nabbed Goat) all parse to `WhoMayActivate:Opponent` correctly; no mislabel.
- `DestroySelfEffectRule.cs` — PASS. "Destroy this Aura" → `DestroyEffect{Target:Self}` (CR 701.8); anchored, redundant type word dropped since `Self` implies it, distinct from `DestroyTargetEffectRule`; replaces the prior unstructured line 3.

## Glossary gaps

None surfaced.

## Process notes

- **Search + implicit shuffle (Maralen line 2):** the oracle ends "…then shuffles," but `SearchLibraryEffect` carries no explicit shuffle field — consistent with the whole codebase, which treats the shuffle as an inseparable consequence of searching a hidden library (CR 701.19c) rather than an independent effect. Not a free-text/unparsed hole; descriptive convention preserved. Noted, not a FAIL.
- **New-discriminator inventory:** only I11's `cantDrawCards` is a genuinely new effect discriminator; I08/I12/I19 all reuse existing discriminators (`cantAttack`, `costReduction`+`castThisTurn`, `enchantRestriction`/`destroy`) via additive fields, an enum value, and a widened branch — so I11 is the only family needing a fresh projection decision, and it has one.

## Result

ALL PASS — PROCEED.
