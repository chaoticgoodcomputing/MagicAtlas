# MAST judge — DELTA verdict (SLICE PB-4 — Bucket A counter-gate)

**Date:** 2026-06-16
**Slice:** PB-4 — Bucket A counter-gate (route hardcoded intervening-if producers through existing ConditionParser.Parse)
**Mode:** DELTA (judge only the change this slice made)
**Gold judged:** MOR/GravelgillAxeshark (uncommitted, working tree)
**Result:** PASS

## Summary

- PASS: 1
- FAIL: 0

## PASS verdicts

- `tests/magic-ast-tests/Fixtures/HandParsedCards/MOR/GravelgillAxeshark.json` — PASS.

### What the slice structured (criterion a)

The Persist intervening-if went:

  OtherCondition{ Text: "it had no -1/-1 counters on it" }
  -> TriggeringObjectCounterCondition{ CounterType: "-1/-1", Present: false }

- Real oracle text (Scryfall oracle-cards.json) matches the fixture Input.OracleText verbatim:
  "Persist (When this creature dies, if it had no -1/-1 counters on it, return it to the battlefield
  under its owner's control with a -1/-1 counter on it.)"
- CR Persist definition ("...if it had no -1/-1 counters on it...") confirms the target node:
  CounterType "-1/-1", Present false. Faithful.
- Producer routed through the EXISTING ConditionParser.Parse — PersistKeyword.cs now calls
  `ConditionParser.Parse(InterveningIfText)` instead of building OtherCondition by hand. The
  pre-existing `TriggeringObjectCounter` regex (ConditionParser.cs L70-72) matches both counter
  texts; ConditionParser.cs has ZERO diff (no new arm added, as the slice required).
- Node citation cross-reference: TriggeringObjectCounterCondition cites CR 603.10 ("the game
  'looks back in time'" for dies-triggers) — exists in rules-structure.json and matches the
  modeling (reads the dying object's last-known counters).

### No new residual (criterion b — PRIMARY)

- No OtherCondition / Kind:"unparsed" / EffectType:"unparsed" / OtherX remain in the ability tree.
- Only free-text strings are Reminder.Text and Oracle.RawText — verbatim-by-design, exempt.
- MOR/GravelgillAxeshark removed from whitelist-freetext.json (along with the other 6 in the set).

### No regression (criterion c)

- Normalized semantic diff of Output.Oracle.Abilities: InterveningIf is the ONLY change.
  Trigger{Dies}, Filter{CardTypes:[permanent],IsSelf}, ReturnToBattlefield, WithCounters{-1/-1,1},
  UnderControl{Owner}, Tapped:false, KeywordSource:Persist, Reminder — all preserved unchanged.
- Off-axis Attributes deltas are regeneration normalizations, not ability drops/inversions:
  colors reordered ["U","B"]->["B","U"] (now aligns with source Colors:["B","U"]), and manaCost
  IsVariable:false default surfaced. Neither is in this slice's InterveningIf axis.

### Out-of-scope residual remaining

None on this gold — its sole InterveningIf was the slice's target and is now structured.

## Process notes

This is a DELTA judgment per slice spec; whole-gold purity not assessed beyond confirming no NEW
residual and no regression. rules-structure.json / glossary.json live under libs/mtg-rules/Data/
(not the path in SKILL.md).

ALL PASS
