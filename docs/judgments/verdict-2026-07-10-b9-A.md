# MAST judge — batch 9 shard A verdict

**Date:** 2026-07-10
**Base:** 7c4a963d
**Scope:** 5 families (5 fixtures, 3 new AST nodes, 1 new coarse-projection entry)
**Result:** PASS

## Summary

- PASS: 6 items (5 family fixtures + 1 projection decision)
- FAIL: 0

## PASS verdicts

- `DemonicConsultation.json` (N05) — PASS. `chooseCardName` + `exile`(top 6) + new **`revealUntilExileRest`** faithfully models the two-sentence coupled reveal-until-chosen-name → hand, remainder EXILED (not bottomed — the material difference from sibling `RevealUntilEffect`, CR 400.4). Chosen name consumed structurally via `ChosenCharacteristic:CardName` (CR 607 linked variable), not free text. CR 701.20a (reveal) + 701.13a (exile) both exist and match. The `Characteristics:[{other,"top"}]` positional predicate is a whitelisted `OtherCharacteristic` residual (no `Position` field on ObjectFilter) matching the UginTheIneffable / MysticForge precedent — an acknowledged irreducible, not a new inline-prose leak.
- `known-coarse-projections.json#revealUntilExileRest` (N05 projection) — PASS. New `effectType` discriminator parked coarse with a justification identical to sibling `revealUntil`: a library-visibility action no flow rule reads yet, consciously inert for interaction recall. A flow rule would not want reveal-until-found; coarse is the correct, sensible choice. Ratchet-required presence is satisfied.
- `MysticalDispute.json` (N11) — PASS. Conditional self-cost-reduction modeled as `static` `costReduction{2}` gated by new **`targetsFilter`** condition composing `ObjectFilter{CardTypes:[spell],Colors:[U]}` (structured axes, not a bespoke color field). Secondary reuses `preventable` + `counterSpell` + `Unless{Controller,{3}}`. CR 118.7 (cost reduction), 601.2c (targets chosen), 601.2f (total cost) all exist and match.
- `SparkDouble.json` (N17) — PASS. Enter-as-copy replacement decomposed as `static` + `When:asThisEnters` + `optional` + reused `becomesCopy`, with new **`CounterAdder`** + **`ConditionalModification`**: +1/+1-if-creature and +loyalty-if-planeswalker each gated via `objectHasCardType`, isn't-legendary via `SupertypeRemover`. Timing is a separate wrapper (no `becomesCopyOnEntry`-style baked-in-timing discriminator). CR 707.2 (copiable values) + 614.12 (enters-as-copy replacement) exist and match.
- `ShiftingBorders.json` (N16) — PASS. `exchangeCharacteristic{Control}` over two target lands + splice-onto-Arcane `{3}{U}` `static` with reminder — both nodes pre-existing (no enum edit). CR 701.12a (exchange control of two permanents) + 702.47 (Splice) exist and match.
- `Fury.json` (N04) — PASS. Evoke as `static` `alternativeCast`(`ExileCost`: red card from hand, CR 702.74a permits non-mana cost) + `triggered` sacrifice-if-evoked (`keywordCostPaid` intervening-if); ETB `dealDamage` 4 `Divided` among `anyAmount` target creatures/planeswalkers; double-strike `combatDamageTiming Both`. CR 702.74a confirms the static+triggered decomposition.

## Sibling sweep (edge-diff offline — sole defense)

- **N05 `^Choose\s+a\s+card\s+name\b` → Spell.** Fires only in the modal-default branch; real modals lead with "Choose one/two —" (bulleted), so no genuine modal collides. Permanent "choose a card name" cards (Runed Halo, Pithing Needle, Nevermore, Meddling Mage) lead with "As … enters," not "Choose," so they are unaffected. No misroute.
- **N11 `^it\s+targets\s+an?\s+(color)\s+spell$` + rule-side `if it targets a (color) spell`.** Fully anchored; the "targets a tapped creature/permanent" sibling arm is disjoint. Five-color coverage is defensive, not a broadening risk. No collision.
- **N16 `"Exchange"` keyword-action.** Leading-verb one-shot; no declarative static ability begins with "Exchange" (control/life-totals/top-card exchanges are all spell/ability effects). No misroute.
- **N04 `_exileColorCardCostPattern`.** Tightly anchored to "Evoke—Exile a [color] card from your hand." (MH2 Incarnations). Mana-cost Evoke preserved as `_manaCostPattern`, so mana-form Evoke (Mulldrifter et al.) still parses; Grief's "Evoke—Discard a card" is explicitly deferred as a separate pattern, not folded in.

## New-node check

`RevealUntilExileRestEffect` (`revealUntilExileRest`), `TargetsFilterCondition` (`targetsFilter`), `CounterAdder` (`counterAdder`), `ConditionalModification` (`conditionalModification`) are all absent from base and branch GLOSSARY.md (regen is post-merge) — confirmed new, non-colliding names, correctly shaped and CR-anchored in their doc-comments.

## Glossary gaps

None.

## Process notes

Projection ratchet categories are `costType`/`effectType`/`restriction`/`triggerEvent` only — Conditions (`targetsFilter`) and CopyModifications (`counterAdder`/`conditionalModification`) are non-projected substructures, so N05's `revealUntilExileRest` is the sole new port-relevant discriminator, and it carries a sensible coarse entry.
