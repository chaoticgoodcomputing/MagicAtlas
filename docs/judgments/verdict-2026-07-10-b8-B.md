# MAST judge — batch 8 shard B verdict

**Date:** 2026-07-10
**Scope:** 7 targets across 5 families — Ward + tokens + cost-reduction (5 gold fixtures, WardKeywordRule.cs extension, TokenDefinition.cs additive field)
**Base:** b74e7983
**Result:** PASS

## Summary

- PASS: 7
- FAIL: 0

## FAIL verdicts

None.

## PASS verdicts

- `tests/magic-ast-tests/Fixtures/HandParsedCards/OvikaEnigmaGoliath.json` — PASS. "Ward—{3}, Pay 3 life." → `CompositeCost[ManaCost{3}, PayLifeCost 3]` on the standard Ward `BecomesTarget`/`preventable`+`counterSpell`/`Unless{ThatPlayer, cost}` shape (CR 702.21a; composite bundling mirrors CR 601.2f). Cast-noncreature trigger → `createToken` Count `derived ManaValue Source "that spell"` (established corpus convention, cf. Niambi/FoodChain) of 1/1 red Phyrexian Goblins, then `gainAbility Haste` `untilTime Turn/End` on `It` (anaphoric to the created group, CR 113.8). No unparsed nodes.
- `libs/magic-ast/Parsing/Parsers/Static/Rules/WardKeywordRule.cs` — PASS. New compound branch's comma-anchored `Ward—{mana}, Pay N life` pattern is tried first and by construction cannot match the four existing forms (bare-mana uses a space not an em-dash; life/sacrifice/discard have no leading `{…}` group). The four forms are byte-identical, only re-nested one `else` deeper. Offline sibling sweep of the 5 Ward fixtures (RedWard, PrismaticWard, etc.): unaffected.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/PlateArmor.json` — PASS. `+3/+3` `modifyPT` + `ward {1}` `gainAbility` (full triggered ability inline) on `EnchantedOrEquipped`; `Equip {3}` `attach` target creature-you-control `OnlyAsSorcery`; the "costs {1} less per other Equipment" reduction is `costReduction Amount1 PerObject{Equipment, You, ExcludeSelf}` with `AppliesTo` null — the documented self-scope convention (CR 118.7, "this ability"). M15 reused the WardKeyword combinator inline and did NOT edit WardKeywordRule.cs (3-file diff confirms). Ward CR 702.21a; P/T + ability-grant layers CR 613.4c / 613.1f.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/MabelHeirToCragflame.json` — PASS. "Other Mice you control get +1/+1" → `modifyPT Each{creature, Subtypes[Mouse], You, ExcludeSelf}` via the new `IrregularPluralSubtypeAnthemRule` (Mice→Mouse), leaving shared `LordPTBuffRule` untouched (CR 611.3, CR 109.5 "other" excludes self). The Cragflame ETB is fully structured: quoted "+1/+1 and has vigilance, trample, and haste" as `modifyPT` + three `gainAbility` keyword grants, plus `Equip {2}`. New additive `TokenDefinition.Supertypes = ["Legendary"]` models CR 205.4a.
- `libs/magic-ast/AST/Effects/TokenDefinition.cs` — PASS. Additive nullable `Supertypes` with `JsonIgnore(WhenWritingNull)`; correctly distinct from `Types`/`Subtypes` per CR 205.4b; non-breaking for every existing token fixture.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/Edgewalker.json` — PASS. Colored-only reduction as `costReduction Amount0 + ManaSymbols[{W},{B}]`; the zero generic component IS "reduces only the amount of colored mana you pay" by construction, so the second sentence needs no separate node (CR 118.7). Scoped by ability-level `AffectedObjects{spell, Cleric, You}`.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/VastwoodZendikon.json` — PASS. `becomesCreature{EnchantedOrEquipped, 6/4, green, CardTypes[land,creature], AddedSubtypes[Elemental]}` — `CardTypes[land,creature]` captures "It's still a land" structurally (no free-text "enchanted land"), CR 205.1b / 613. "When enchanted land dies" → `Trigger{Dies, Filter{land, IsEnchanted}}` + `returnToHand It` (CR 603.2, CR 700.4 "dies"). All structured.

## Projection (initiative 03)

No branch introduces a new effect/cost type, trigger event, or restriction discriminator — every family reuses existing AST nodes (CompositeCost/PayLifeCost, ModifyPT/GainAbility/CostReduction, BecomesCreature/returnToHand) or adds pure data (TokenDefinition.Supertypes). No branch touches PortGraph/PortWalkProjection/known-coarse-projections.json, consistent with the exhaustiveness ratchet not demanding a new projection entry. No projection verdicts required.

## Glossary gaps

None surfaced.

## Process notes

- **Colorless-token `Colors: ["C"]` sentinel (Mabel / Cragflame).** The token is "legendary colorless Equipment"; the fixture encodes colorless as `Colors: ["C"]`. The SKILL flags `["C"]` colorless encodings, but this is a **documented, corpus-wide sentinel convention**, not an assertion that C is a color: the M03 rule in this same batch comments `// CR 105.1: colorless is not a color; the token's Colors list carries the "C" marker, mirroring the sibling rule's convention`, and base `b74e7983` already ships it in merged fixtures (MyrelShieldOfArgive, StoneIdolTrap, SpringleafParade). It is out of M18's stated scope and predates this branch, so it does NOT block the batch. Flagged for a possible corpus-wide normalization to `Colors: []` (CR 105.2a) as a separate cleanup, not a per-item FAIL.
- **`Source: "that spell"` (Ovika X-token count) and `Target: {Kind: It}` for a plural token group** are both established corpus conventions (Niambi `"that creature"`, FoodChain `"exiled creature"`; Repentance `Source: "it"`), not novel escape hatches — no FAIL.
