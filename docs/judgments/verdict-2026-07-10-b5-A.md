# MAST judge — batch 5, shard A verdict

**Date:** 2026-07-10
**Base sha:** c4f2a3e6
**Scope:** 5 families (5 fixtures, 4 new AST nodes, 3 new/edited rules, 1 shared-file edit, 2 coarse-projection entries)
**Result:** PASS

## Summary
- PASS: 7 verdict items (5 family core + 2 projection)
- FAIL: 0

## Per-family verdicts

### J05 — Herald of Eternal Dawn — PASS
`HeraldOfEternalDawn.json` + `CantLoseGameEffect.cs` + `CantWinGameEffect.cs`
- Third static ability splits correctly by scope: `cantLoseGame` -> `You`, `cantWinGame` -> `EachOpponent`. Matches the oracle "You can't lose the game and your opponents can't win the game."
- Secondary lines structural: Flash -> `timingModification Grant Instant`; Flying -> `evasion`. No unparsed.
- New nodes are genuinely new and correctly shaped (`Player: ObjectReference`), distinct from `CantLoseGameForZeroLifeEffect` (narrow 0-life scope) and `WinTheGameEffect` (a player wins).
- CR 104.2a (win / opponents-left) and CR 604.1 (static ability) quoted verbatim; CR 104.3 (ways to lose) and CR 704.5a (0-life SBA) exist and match.
- **Projection:** two new effect discriminators parked coarse with sensible reasons (game-outcome rules-of-the-game statics, no flow rule reads loss/win overrides, genuinely inert). PASS.

### J13 — Verdant Mastery — PASS
`VerdantMastery.json` + `AlternativeCostPaidCondition.cs` + `RemainderQuantity.cs` + `SearchPlacement.cs` (shared)
- Alt cost "You may pay {3}{G} rather than pay this spell's mana cost" reuses `grantAlternativeCost` with `AffectedSpells.IsSelf` (Fist of Suns precedent), CR 118.9 verbatim.
- Search line faithfully distributed: opponent share (1, `BattlefieldTapped`, gated on `alternativeCostPaid`), your share (2, `BattlefieldTapped`), remainder -> `Hand`; basic lands; `Revealed: true`.
- "Then shuffle." is implicit family-wide — verified against the `Cultivate` (M11) precedent fixture, which likewise carries no shuffle field. Not a lossy drop.
- `SearchPlacement` gains nullable `Controller` + `Condition` with `JsonIgnore(WhenWritingNull)` — Cultivate / Kodama's Reach fixtures are byte-identical (they carry neither field). No sibling break.
- New nodes are field-less markers; schema additions carry `IsUnparsed: false`. CR 118.9 / 601.2f / 601.2b / 604.5 / 701.23 all exist and match the modeling.
- Minor (not a FAIL): `RemainderQuantity` doc cites "701.23e-style" (701.23e is the reveal default, not the distribution) — within-rule subrule imprecision, doctrine says do not nitpick.
- No new PortWalk discriminator (a Condition + a Quantity kind); projection N/A.

### J15 — Psychic Vortex — PASS
`PsychicVortex.json` + `Cost.cs#DrawCardsCost`
- Cumulative upkeep decomposed per CR 702.24a: `putCounters age (1) on Self`, then `preventable { sacrifice Self } unless pay scaledCost{ PerUnit: drawCards(1), Count: age-counters-on-self }` — the batch-1 pay-life precedent (Inner Sanctum) widened to a draw cost.
- New `DrawCardsCost` (`drawCards` costType) is genuinely new, count-only, mirrors `PayLifeCost`; CR 702.24a + 121.1 match.
- Secondary end-step trigger fully structured: composite `sacrifice land` + `discardCards CardsInHand (You)`.
- **Whitelisted debt (noted, not a FAIL):** the cumulative-upkeep `InterveningIf` is `ConditionType: "other"` free-text ("this permanent is on the battlefield"). This is the CR 702.24a keyword boilerplate carried identically by the accepted precedent Inner Sanctum (present on base c4f2a3e6) and dozens of merged golds; it is an explicit PB-7 entry in `whitelist-freetext.json` (tag `debt`, burndown plan) enforced by the stateless `FreeTextWhitelistTests` gate. It is not a newly-introduced un-whitelisted residual on the judged draw-cost work, and FAILing it would apply a stricter standard than its own cited, already-merged precedent.
- **Projection:** `costType.drawCards` parked coarse — sensible (sibling of coarse payLife/payEnergy costs; mirrors the already-coarse `drawCards` EffectType; no flow rule reads a draw-as-cost). PASS.

### J11 — Moderation — PASS
`Moderation.json` + `CantCastMoreThanNSpellsRule.cs` (shared, broadened)
- N=1 modeled: `cantCastMoreThanNSpells` (`You`, `MaxPerPeriod: 1`, `Period: eachTurn`). Secondary "Whenever you cast a spell, draw a card" -> `SpellCast (Controller You)` trigger + `drawCards 1`. No unparsed.
- **Sibling sweep** of the broadened anchored regex `(Each player|You) can't cast more than N spell(s) each turn`: newly and correctly captures Colfenor's Plans and Yawgmoth's Agenda (both genuinely "You can't cast more than one spell each turn."). Correctly EXCLUDES Hedonist's Trove ("...one spell **this way** each turn"), Curse of Exhaustion ("**Enchanted player**..."), Deafening Silence / Phyrexian Censor ("...one **noncreature/non-Phyrexian** spell..."), I Bask in Your Silent Awe ("**Each opponent**..."). Zero mislabels.
- CR 601.3a exists (parent 601.3 is marginally more on-point for a plain casting prohibition, but subrule imprecision is not a FAIL); CR 611.1 / 601.2 match. Reuses an existing discriminator; projection N/A.

### J01 — Quintorius, Field Historian — PASS
`QuintoriusFieldHistorian.json` + `CardsLeaveGraveyardAggregateConditionRule.cs`
- Aggregate trigger "Whenever one or more cards leave your graveyard" modeled as `LeavesGraveyard` + `Filter{ CardTypes:["card"], Controller: You }` + `MinimumCount: 1` (CR 603.2 aggregate, verbatim). Effect `createToken` 3/2 R+W Spirit. Anthem "Spirits you control get +1/+0" -> `modifyPT` structural. No unparsed.
- Reuses the existing `LeavesGraveyard` event (Syr Konrad precedent) + `MinimumCount` convention — no new discriminator, projection N/A.
- **Sibling sweep** of `^one or more cards? leave your graveyard$`: correctly captures ~19 genuine untyped aggregate triggers (Amzu, Ark of Hunger, Tormod, Willow Geist, On Wings of Gold, both Quintorius, Teval, etc.); correctly EXCLUDES type-restricted ("creature/artifact cards" — Desecrated Tomb, Chalk Outline, Imotekh, Dredger's Insight), temporally-qualified ("during your turn" — Attuned Hunter, Kheru Goldkeeper), and singular ("a card" — Syr Konrad, Kishla Skimmer, Murktide Regent, Oasis of Renewal). Zero mislabels.
- Minor: filter uses `CardTypes: ["card"]` sentinel to mean untyped "any card" — a deliberate structured convention (doc-comment intentional), not free-text; acceptable.

## Glossary gaps
None.

## Process notes
- The zsh `:h/:t/:l` history-modifier expansion mangles `git show <branch>:<path>` when the branch var is unbraced and immediately followed by `:path`; used quoted full refs.
- J15's whitelisted `OtherCondition` intervening-if is the single item worth a second look. It is the shared cumulative-upkeep skeleton, not a new hole; the free-text policy for it is enforced by the whitelist gate, not by this judge. Called out for transparency.

**ALL PASS**
