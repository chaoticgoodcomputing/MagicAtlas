# MAST judge — batch verdict

**Date:** 2026-06-16
**Scope:** 7 items (4 fixtures, 3 AST/parser nodes) — slice PB-1 (aura IsEnchanted + BearUmbra full re-derive)
**Result:** PASS

## Summary

- PASS: 7
- FAIL: 0

## FAIL verdicts

None.

## PASS verdicts

- `libs/magic-ast/AST/References/ObjectFilter.cs#IsEnchanted` — PASS. Flat `bool?` axis for the Aura's currently-attached permanent. CR 303.4b: "The object or player an Aura is attached to is called enchanted." Mirrors `IsSelf`/`IsToken`; doc cites CR 303.4 / 702.5, both present in the rules data and matching.
- `libs/magic-ast/AST/References/KeywordAbility.cs#UmbraArmor` — PASS. New enum member with Oracle member name "Umbra armor". CR 702.89 names the keyword "Umbra Armor"; CR 702.89b: older cards' text "has been updated in the Oracle card reference to refer to umbra armor instead." A distinct member (rather than reusing the obsolete `TotemArmor`) faithfully records the card's current Oracle wording.
- `libs/magic-ast/Parsing/Parsers/Static/Rules/EnchantedPTAndGrantedAbilityRule.cs` — PASS. Decomposes "Enchanted creature gets +N/+M and has \"[ability]\"" into a `CompositeEffect` of `modifyPT` + `gainAbility`, both on `EnchantedOrEquipped`. CR 303.4e: an Aura that grants an ability via "gains"/"has" confers it to the enchanted object. Timing is not baked into an effect — the granted ability is a full nested ability node (triggered here), the canonical composite shape.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/ROE/LuminousWake.json` — PASS. The AttacksOrBlocks trigger filter is now `CardTypes:["creature"] + IsEnchanted:true` (CR 303.4b — the attached creature), replacing the `OtherCharacteristic{"enchanted"}` free-text. Sibling axes (Event, gainLife 4 → You) preserved.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/M14/UnhallowedPact.json` — PASS. The Dies trigger filter is now `CardTypes:["creature"] + IsEnchanted:true`; `returnToBattlefield {Target:It, UnderControl:You}` preserved; no `OtherCharacteristic` remains.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/ROE/BearUmbra.json` — PASS. Full re-derive correct on all four prior defects: (a) magnitude is **+2/+2** (was wrongly +3/+3); (b) the untap-lands ability is a **granted** `gainAbility` on `EnchantedOrEquipped` (was wrongly a self-trigger on the Aura), whose `GainedAbility` is the triggered "Whenever this creature attacks → untap Each {land, Controller:You}"; (c) the granted trigger's self-reference is `IsSelf:true` — the granted ability's own source (CR 303.4e / 109), not a separate "enchanted creature" filter; (d) keyword is **Umbra armor** (CR 702.89), not the obsolete Totem armor. No `OtherCharacteristic`, no `unparsed`. Input was re-pointed to the Scryfall text (matches the corpus byte-for-byte), not deleted.
- `tests/magic-ast-tests/Fixtures/HandParsedCards/ROE/HyenaUmbra.json` — PASS. Collateral of the global keyword fix: keyword re-points Totem armor → Umbra armor (CR 702.89, current Oracle name). The +1/+1 + first-strike grant is structurally unchanged; this is a faithfulness improvement, not a regression.

## Glossary gaps

None. "Umbra Armor" and "Totem Armor (Obsolete)" are both present in glossary.json.

## Process notes

- The `IsEnchanted` axis is the filter-level dual of the reference-level `EnchantedOrEquipped` kind — appropriate: triggers/effects whose subject IS the attached creature ("whenever enchanted creature attacks") want a filter predicate, while the modifyPT/gainAbility targets use the reference kind. Both appear correctly in Bear Umbra (reference kind for the buff/grant targets) and in Luminous Wake / Unhallowed Pact (filter predicate for the trigger subject).
- No new top-level discriminator was added (IsEnchanted is a bool field on an existing record; UmbraArmor is an enum member), so no PortWalk projection decision is required — the full NUnit suite is green (4595 passed) including any schema-export checks.
