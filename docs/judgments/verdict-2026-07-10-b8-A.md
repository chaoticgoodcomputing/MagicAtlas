# MAST judge — batch 8 shard A verdict

**Date:** 2026-07-10
**Base:** b74e7983
**Scope:** 13 items across 5 new-node families (6 fixtures, 5 AST nodes, 2 projection decisions)
**Result:** PASS

## Summary

- PASS: 13
- FAIL: 0

## Per-family verdicts

### M20 / spree-modal-additional-cost (Requisition Raid)
- `AST/Abilities/SpreeModeAbility.cs` — PASS. New `spreeMode` ability pairs one `AdditionalCost` + `Effects` per printed "+ [cost] — [effect]" mode; CR 702.172a (Spree = static on modal spells, "choose one or more additional costs") exists and matches. Doc-comment's inline quote is an older paraphrase but the citation is valid, not contradictory.
- `Fixtures/HandParsedCards/RequisitionRaid.json` — PASS. All 3 modes structured with AdditionalCost {1} + a structured effect: destroy target artifact / destroy target enchantment / putCounters +1/+1 on Each creature `Controller:Target`. Matches the real BLB oracle verbatim. No unparsed.
- `Fixtures/HandParsedCards/BLB/WildcallSpree.json` (BACK-PROP) — PASS. The prior two `unparsed` mode nodes are replaced by two structured `spreeMode`+`createToken` modes ({1}->one 1/1 green Squirrel; {2}->X 1/1 green Squirrels), faithful to the fixture's own `Input.OracleText`; the `whitelist-unparsed.json` entry is correctly removed. **Fidelity note:** this is a *synthetic* grammar-test fixture — its `Input.Name` "Wildcall" collides with the real Bloomburrow **Wildcall** (a Manifest card, not Spree), and no real card has this Spree/Squirrel text. The `Input` is pre-existing and unmodified by this branch, so this is not a defect the branch introduces; the back-prop faithfully models the stated input and is orchestrator-blessed. Flagged for awareness only.

### M04 / multi-type-anthem-food-golem (Brenard, Ginger Sculptor)
- `AST/Effects/TokenCopy/CopyModification.cs#ActivatedAbilityAdder` — PASS. New `activatedAbilityAdder`, the activated analogue of `TriggeredAbilityAdder`, carries a structured `ActivatedAbility` (not free text). CR 707.2 (copiable values) and CR 602.1 ([Cost]:[Effect]) valid.
- `Fixtures/HandParsedCards/BrenardGingerSculptor.json` — PASS. Anthem "a Food or a Golem" encoded as `Subtypes:[Food,Golem]` — the **established corpus disjunction convention** (verified present on-base in `MultiSubtypeAnthemModifyPTRule`/Kirri; CR 205.3), so OR-semantics, not a novel misencoding. +2/+2 and trample as two effects in one static (CR 611.3). Dies->exile->copy modeled as `optional{Inner:exile; IfYouDo:copy}` with pt-override 1/1, `typeAdder` [artifact,creature]+[Food,Golem] for "in addition to its other types" (CR 205.1b matches exactly), and `activatedAbilityAdder` for the quoted "{2},{T},Sacrifice: gain 3 life" ability. Fully structured, faithful, no free text.

### M13 / self-regenerate-replacement (Clergy of the Holy Nimbus)
- `AST/Effects/ZoneChange/CantBeRegeneratedEffect.cs` — PASS. New `cantBeRegenerated` ContinuousEffect, nullable-Target=self convention (mirrors CantAttack/CantHaveCounters), well-named. CR 701.19 (Regenerate) valid.
- `Fixtures/HandParsedCards/ClergyOfTheHolyNimbus.json` — PASS. Self-regen as a replacement of the destruction event (`OriginalEventOccurs:false`, `regenerate Self`; CR 614.1/701.19). Activated {1} `cantBeRegenerated` this-turn with `WhoMayActivate:"Opponent"` for "Only your opponents may activate this ability" (reused field). Faithful, no unparsed.
- `known-coarse-projections.json#cantBeRegenerated` — PASS. Coarse projection sensible: regeneration-denial is a preventive object-suppression restriction, not an emit/consume loop event; no flow rule wants it. Genuinely inert.

### M19 / cant-play-lands (Aggressive Mining)
- `AST/Effects/Timing/CantPlayLandsEffect.cs` — PASS. New `cantPlayLands` Effect with required `Player` scope (sibling of CantDrawCardsEffect); Player kept as a field rather than baked into the discriminator (correct — supports symmetric/asymmetric variants). CR 305.1/116.2a (playing a land = special action) valid.
- `Fixtures/HandParsedCards/AggressiveMining.json` — PASS. `cantPlayLands Player:You`; activated `sacrifice` land (qty 1) -> `drawCards` 2, `Restrictions:["OnlyOnceEachTurn"]`. Faithful, no unparsed.
- `known-coarse-projections.json#cantPlayLands` — PASS. Coarse projection sensible: a rules-of-the-game prohibition on the special action of playing a land, not a loop emit/consume event. Genuinely inert.

### M02 / self-inclusive-enters-disjunction (Arahbo, the First Fang)
- `Parsing/Parsers/Triggered/Rules/SelfOrAnotherNontokenSubtypeEntersConditionRule.cs` — PASS. Produces a **self-inclusive** filter (NO `ExcludeSelf`) per CR 603.2 for "[Name] or another nontoken [Subtype] you control enters". Anchored `^.+ or another nontoken [Capitalised Subtype] you control enters$` plus a `lower.Contains("or another nontoken")` guard.
- `Fixtures/HandParsedCards/ArahboTheFirstFang.json` — PASS. "Other Cats" anthem `ExcludeSelf:true`; enters trigger `Subtypes:[Cat] IsToken:false Controller:You` with NO `ExcludeSelf` (Arahbo's own entry triggers it). createToken 1/1 white Cat. Faithful.

## Sibling sweep (edge-diff offline — manual is sole defense)

`or another nontoken [Subtype] you control enters` matcher, swept vs oracle bulk:
- **Claimed (all genuinely self-inclusive, correct):** Sethron Hurloon General, Darksteel Splicer ("this creature or another"), Go-Shintai of Life's Origin, Arahbo. All are "[Name/this creature] or another" — self-inclusive by design, so NO-ExcludeSelf is right for every one.
- **NOT stolen (correctly excluded by the `or another` guard, keep ExcludeSelf):** Lathliss Dragon Queen, Inalla, Miirym, Black Panther Vanguard, Knightfisher, Baboon Spirit — all bare "Whenever another nontoken [Subtype] you control enters" (no "or another"). Guard returns null; generic EntersConditionRule applies ExcludeSelf. No theft.

Brenard `Subtypes:[Food,Golem]` OR-convention swept vs on-base precedent (`MultiSubtypeAnthemModifyPTRule`, Kirri) — consistent.

## Glossary gaps

None.

## Process notes

- **Schema-sync inconsistency (build concern, not a rules FAIL):** the spree and Brenard branches updated `libs/magic-ast/schema/ast-schema.json` for their new discriminators, but the self-regenerate and cant-play-lands branches did NOT register `cantBeRegenerated` / `cantPlayLands` in the schema (base enumerates effect discriminators, e.g. `cantDrawCards`). This is squarely NUnit/build territory (out of judge scope) and is likely picked up by the batch-level "advance discriminator baseline / schema regen" chore at merge — the orchestrator should confirm that regen covers both new effects before merging.
- **WildcallSpree** is a synthetic fixture with a real-card name collision (see M20). Pre-existing Input; back-prop is faithful. Surfaced for awareness, not a FAIL.

**Result: ALL PASS.**
