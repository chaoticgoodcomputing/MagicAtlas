# MAST judge — batch-2 (2026-07-10) SHARD B verdict

**Date:** 2026-07-10
**Base sha:** b8348b1c
**Scope:** 4 branches, 21 targets (4 fixtures + 17 AST/parser/data targets)
**Result:** PASS

## Summary

- PASS: 21
- FAIL: 0

## FAIL verdicts

None.

## PASS verdicts

### Branch 1 — choose-landtype-as-enters (G06) — CR 614.12 / 305.6 / 607
- `RootsOfLife.json` — PASS. Line 1 is a *static* replacement (`When:asThisEnters`) carrying a **plain** `chooseBasicLandType` effect with `Options:["Island","Swamp"]` (the literal printed subset, a real game-affecting restriction preserved). Line 2 binds "of the chosen type" via structured `ChosenCharacteristic:BasicLandType` (a CR 607 back-ref, **not** free text). Timing is not baked into the effect discriminator.
- `ChooseBasicLandTypeEffect.cs` — PASS. Plain choice-declaration; `Options` records the printed subset (correctly divergent from full-set `chooseCardType` which drops its enumeration). CR 305.6 constrains values to the five basic land types.
- `ChooseBasicLandTypeOnEntryRule.cs` — PASS. Emits `StaticAbility{When=AsThisEnters}` + plain effect; anchored to basic-land-type words. Sibling `ChooseColorOnEntryRule` confirmed **untouched** (new sibling, not an edit).
- `LandOfChosenTypeBecomesTappedConditionRule.cs` — PASS. Anchored to "a land of the chosen type … becomes tapped"; runs before the generic `BecomesTappedConditionRule` so the chosen-characteristic ref is not dropped. No sibling mislabel — the regex requires the literal "of the chosen type".
- `known-coarse-projections.json#chooseBasicLandType` — PASS (projection decision). A basic-land-type *choice declaration* is genuinely inert for interaction recall (no flow rule reads choice-declaration events); the consumer trigger is a separate node. Consistent with the already-coarse siblings `chooseCardType`/`chooseColor`/`chooseCreatureType`. Coarse is sensible.

### Branch 2 — protection-from-chosen-color (G10) — CR 702.16b / 607 / 205.3h / 701.21a
- `FloatingShield.json` — PASS. All 4 lines parse. The doctrine call is correct: "the chosen color" on both the enchanted-creature grant (line 3, with `DoesNotRemoveThisAura`) and the sac-activated grant (line 4) is modeled as `ChosenCharacteristic:Color` — the DEFINITE CR 607 back-reference to the single color bound at enters — correctly distinct from `ChosenColor` (a fresh per-resolution choice, Mother of Runes).
- `ProtectionQualityKind.cs#ChosenCharacteristic` — PASS. New value doc-distinct from `ChosenColor`.
- `ProtectionQuality.cs#ChosenCharacteristic` — PASS. Axis field mirrors `ObjectFilter.ChosenCharacteristic`; structured.
- `SacrificeThisAuraCostRule.cs` — PASS. Aura enchantment-subtype (CR 205.3h) sac cost as `CardTypes=[enchantment]+Subtypes=[Aura]+IsSelf`; anchored `^Sacrifice this Aura$` above the shared helper, so no sibling sacrifice cost is mislabeled.
- `TargetGainsProtectionFromChosenColorActivatedEffectRule.cs` — PASS. Anchored; `ChosenCharacteristic:Color` back-ref; carries no you-control filter (matches oracle, unlike Mother of Runes).
- `EnchantedHasProtectionRule.cs` (own-file edit) — PASS. Adds the "the chosen color" → `ChosenCharacteristic` branch; existing color/`Everything` branches intact.

### Branch 3 — life-threshold-conditional-static (G12) — CR 702.12a / 611.2 / 119.1
- `BaneLordOfDarkness.json` — PASS. Line 1: static `asLongAs` (current `LifeTotal` ≤ half `StartingLifeTotal`) granting indestructible on `Self`. Line 2: dies-trigger filter `creature, IsToken=false, Controller=You, ExcludeSelf=true`; consequent is a faithful nested `optional{Chooser=target opponent, Inner=you draw, IfYouDoNot=you-may put creature from hand with toughness ≤ ThatCreature}`. No `unparsed`, no free text.
- `Quantity.cs#StartingLifeTotal` — PASS. New `DerivedKind` for the fixed starting total (CR 119.1), correctly distinct from the current `LifeTotal`.
- `ConditionParser.cs#LifeTotalVsHalfStarting` — PASS. `Left=LifeTotal` (current), `Right=half(StartingLifeTotal)`; operators mapped faithfully; anchored.
- `TriggeredRuleHelpers.cs` ("another nontoken creature") — PASS. **Highest-risk edit, verified safe.** `ExcludeSelf` is set ONLY when the literal substring "another nontoken creature" is present, else `null` — byte-identical to prior behavior for plain "nontoken creature". Corpus grep confirms no existing fixture regresses: `TheGreatHenge`, `NimDeathmantle`, `AzulaCunningUsurper` use "a nontoken creature" and `SatyaAetherfluxGenius` uses "other target nontoken creature" — none contains "another nontoken creature", so all keep `ExcludeSelf=null`.
- `AsLongAsLifeThresholdSelfKeywordGrantRule.cs` — PASS. Card-name subject → `Self` back-ref (CR 201.5); condition wrapped in `AsLongAsDuration`; anchored, disjoint from the pronoun-form `AsLongAsStaticGrantRule`.
- `TargetOpponentMayDrawElsePutCreatureFromHandRule.cs` — PASS. Fixes the LOSSY line-2 parse (previously `DrawCardsTriggeredRule` swallowed chooser + fallback); anchored, priority above the generic draw rule.

### Branch 4 — x-damage-divided-pay-x-life (G19) — CR 601.2d / 601.2f
- `FireCovenant.json` — PASS. Spell `dealDamage Amount=X, Divided=true, Target=creature anyAmount`; `additionalCosts` = `PayLifeCost Amount=X, IsOptional=false`. Matches CR 601.2d ("divide or distribute an effect … among … targets") and CR 601.2f (additional cost).
- `DealDamageEffect.cs#Divided` — PASS. Additive bool recording the divide-among-targets fact (601.2d quoted verbatim in doc-comment). Structured, not free-text or timing-baking; no new discriminator introduced.
- `AbilityClassifier.cs` (divided → Spell) — PASS. Anchored on the literal "divided … among" surface and excludes quoted text; does NOT reroute sibling "deals N damage to" cards.
- `AttributeExtractor.cs` (pay-X-life) — PASS. Reuses `PayLifeCost` with `VariableQuantity X`; anchored; sacrifice-family branch untouched.

## Glossary gaps

None surfaced. All terms (protection, indestructible, starting life total, basic land type, Aura, sacrifice) are standard MTG-domain terms.

## Process notes

- **CR cross-reference:** every cited rule was confirmed present in `rules-structure.json`. Load-bearing verbatim matches: 601.2d (divide/distribute among targets), 702.16b (protection can't be targeted by the stated quality), 614.12 (as-enters replacement), 305.6 (five basic land types), 119.1 (starting life total 20), 611.2 (conditional continuous effect), 702.12a (indestructible is a static ability), 205.3h (Aura enchantment subtype), 701.21a (sacrifice cost). The worker's re-cite of 601.2d over the brief's 601.2b for the divided-damage fact is confirmed correct.
- **Linked-ability doctrine (branches 1 & 2):** both `ChosenCharacteristic` treatments use a *structured* reference to the linked-ability variable, NOT the free-text `Characteristics:["of the chosen type"]` shortcut the doctrine FAILs. The producer effects are plain (timing on the `StaticAbility.When` axis), not `Choose*OnEntryEffect` timing-baked discriminators. This is the mature evolution the brief frames as correct — no FAIL.
- **Projection ratchet:** only branch 1 introduced a new PortWalk discriminator (`chooseBasicLandType`) and correctly parked it as coarse. Branches 2 (enum value on `protection`), 3 (`DerivedKind` + condition branch), and 4 (additive `Divided` bool + reclassification) introduce no new effect/cost/trigger discriminator, so no projection entry was required and none was added — consistent with the ratchet.

**Result: ALL PASS.**
