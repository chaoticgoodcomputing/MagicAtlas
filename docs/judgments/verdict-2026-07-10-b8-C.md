# MAST judge — batch 8 shard C verdict (anthems + statics)

**Date:** 2026-07-10
**Base:** b74e7983
**Scope:** 13 targets (5 fixtures, 7 new/edited AST rule files, 1 classifier intercept)
**Result:** PASS

## Summary

- PASS: 13
- FAIL: 0

## FAIL verdicts

(none)

## PASS verdicts

### M01 — supertype-subtype-anthem (General's Enforcer)
- `GeneralsEnforcer.json` — PASS. "Legendary Humans you control have indestructible" -> gainAbility over `Each` with AND-ed `Supertypes:[Legendary]` + `Subtypes:[Human]` + `Controller:You`, granting a `static` Indestructible keyword ability (CR 702.12a "Indestructible is a static ability", CR 611.3). Activated half: `exile` target graveyard card + `ConditionalEffect(ObjectHasCardTypeCondition Subject=It)` -> `createToken` 1/1 W Human Soldier. Fully structured; no unparsed/free-text.
- `SupertypeSubtypeKeywordGrantRule.cs` — PASS. Anchored two-capitalised-word grant; **closed-supertype guard verified** — vocab set {Commander, Legendary, Snow, Basic, World, Ongoing} declines non-supertype adjectives (Untapped/Equipped route to their owning sibling rules), so it cannot fabricate a bogus supertype for this class. Keyword resolved via shared `MapKeywordToStaticAbility` (declines cleanly if unsupported).
- `ConditionalCreateTokenIfCardTypeEffectRule.cs` — PASS. Structured `ObjectHasCardTypeCondition` (Subject=It) gating `CreateTokenEffect`; last-known-info citations CR 109.2 / CR 608.2h confirmed and apposite.

### M05 — fateful-hour-conditional-anthem (Gavony Ironwright)
- `GavonyIronwright.json` — PASS. `modifyPT` +1/+4 over `Each` creature you control **ExcludeSelf** with `AsLongAsDuration` whose condition is `quantityComparison` LifeTotal(you) **LessThanOrEqual 5**. Ability word "Fateful hour" carried as a descriptive label only (CR 207.2c — confirmed listed ability word, no rules meaning). +1/+4 and ExcludeSelf both correct.
- `AsLongAsOtherCreaturesPTBuffRule.cs` — PASS. Leading `As long as <cond>,` form; condition via `ConditionParser` wrapped in `AsLongAsDuration`; mandatory `Other` -> ExcludeSelf; anchored, explicitly disjoint from `LordPTBuffRule` (which declines "as long as").

### M11 — chosen-creature-type-anthem (Adaptive Automaton)
- `AdaptiveAutomaton.json` — PASS. Three linked abilities (CR 607.1): producer `chooseCreatureType` under `When: asThisEnters` (timing correctly split from the effect, NOT a baked-in `chooseCreatureTypeOnEntry` discriminator); consumer `addType` Self via `ChosenSubtypeReference:CreatureType`; and the **new** `modifyPT` +1/+1 over `Each` creature you control with structured `ChosenCharacteristic:CreatureType` + ExcludeSelf. Linked-variable is a **structured reference**, never the `Characteristics:["of the chosen type"]` free-text anti-pattern.
- `OtherChosenTypeAnthemModifyPTRule.cs` — PASS. `ChosenCharacteristicKind.CreatureType` structured consumer + ExcludeSelf; anchored, disjoint from the noncreature-source `ChosenTypeAnthemModifyPTRule`.

### M12 — controlled-creatures-cant-attack (Glacial Chasm)
- `GlacialChasm.json` — PASS. All four abilities faithful. Cumulative-upkeep trigger: age counter + `preventable(sacrifice) unless scaledCost(payLife 2 per age counter)` — an exact reformulation of **CR 702.24a**, whose text ("...**if this permanent is on the battlefield**, put an age counter...") also validates the intervening-if. The intervening-if is a free-text `OtherCondition`, **whitelisted as PB-7 debt** with a genuine precedent (Inner Sanctum + Psychic Vortex both carry the identical sink/reason — verified in whitelist-freetext.json). ETB-sac-land, `cantAttack` (Each creature Controller=You), and `preventDamage` (All, Target You) all correct.
- `CreaturesYouControlCantAttackRule.cs` — PASS. `CantAttackEffect` control-filtered, always-on (no duration); CR 508.1c ("restrictions...say a creature can't attack") confirmed. Anchored full-literal, disjoint from without-keyword sibling.
- `PreventAllDamageToYouStaticRule.cs` — PASS. `PreventDamageEffect All=true Target=You`, no Source restriction; CR 615.1/615.1a/604.2 all confirmed verbatim. Anchored, strips reminder.
- `AbilityClassifier.cs` (prevent-all-damage-to-you intercept) — PASS. Pure insertion; word-boundary anchor after "you" (no trailing "control") keeps it disjoint from the creatures-you-control classifier path; correctly classifies as Static (CR 604.2) rather than the imperative spell-verb branch. (Region-level merge disjointness vs the M06 edit to this same file is the orchestrator's concern; the M12 edit is a self-contained new `if` block.)

### M17 — opponents-creatures-abilities-cant-activate (Linvala, Keeper of Silence)
- `LinvalaKeeperOfSilence.json` — PASS. Flying evasion (CanBeBlockedBy Flying/Reach, CR 702.9b) + `cantActivateAbilities` over `AffectedObjects` creatures Controller=Opponent. **Total-lock modeling verified**: no `ExceptManaAbilities` carve-out present, matching an oracle that carries no "unless they're mana abilities" clause — mana abilities are locked too.
- `OpponentCreaturesActivatedAbilitiesLockedRule.cs` — PASS. Creature-scoped opponent activation lock; anchored, disjoint from artifact/chosen-name siblings. CR parent **602.5** ("A player can't begin to activate an ability that's prohibited from being activated") is the exact match; doc's `602.5c` is a parent-vs-subrule imprecision (per skill, not a FAIL).

## Sibling sweep (edge-diff offline)

All five new static/anthem rules are `^...$`-anchored on their full literal templates with explicit disjointness from named siblings:
- M01 requires two capitalised words + closed supertype guard (can't steal "creatures you control..." or state-adjective clauses).
- M05 mandates leading "As long as" + "Other creatures you control" + signed P/T (yields to LordPTBuffRule).
- M11 requires literal "of the chosen type" (disjoint from noncreature-source variant).
- M12 both rules full-literal; classifier intercept word-boundary-guarded vs "creatures you control".
- M17 requires "creatures your opponents control" (disjoint from artifact/chosen-name locks).
No over-broad regex that would mislabel a sibling card was found.

## Glossary gaps

(none — "fateful hour" is a listed CR 207.2c ability word; "cumulative upkeep" is CR 702.24)

## Process notes

- **M01 supertype vocab over-inclusion (non-blocking):** the closed set inherited from `SupertypeAnthemAndKeywordGrantRule` includes "Commander", which is not a CR 205.4a supertype (basic, legendary, ongoing, snow, world). It does not affect General's Enforcer (which uses Legendary) and cannot produce an incorrect gold for this fixture, so it is not a FAIL — but the mirrored set carries a latent bogus-supertype entry that a future card could trip. Surface to code review, not a rules-accuracy blocker.
- **M12 "sacrifice a land" (non-blocking):** the reused ETB-sac ability models the target as `Any` land with no `Controller:You`; the control restriction is a CR 701.16 consequence (not printed text), so this reused pattern is acceptable.
- **M12 whitelist:** the GlacialChasm `OtherCondition` debt entry is precedent-consistent with two existing sibling cumulative-upkeep printings; the free-text content is a verbatim CR 702.24a intervening-if, i.e. sanctioned tracked debt, not a novel hole.
