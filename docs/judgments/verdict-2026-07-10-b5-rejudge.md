# MAST judge — batch 5 RE-JUDGE (remediation of 5 FAILs)

**Date:** 2026-07-10
**Branch:** feat/loop-trial @ b160e112 (remediation commit)
**Scope:** 5 remediated families (J02, J03, J04, J16, J17)
**Result:** PASS (5/5)

## Summary

- PASS: 5
- FAIL: 0

All five original FAILs are correctly remediated. Two were substantive parse fixes
(J02 self-inclusive dies-trigger, J17 subtype-decline guard) and three were citation
corrections (J03, J04, J16). No residual wrong/fabricated citations, and the J17
decline set introduces no false-positive subtype drop.

## PASS verdicts

- **J02 — Anax, Hardened in the Forge** — PASS. `TriggeredRuleHelpers` nontoken branch now guards
  `isDisjunction = lower.Contains("or another nontoken creature")` and only sets `ExcludeSelf` when
  NOT a disjunction — mirroring the plain-creature Guard 2 (Zulaport). Anax gold dies-trigger is now
  self-inclusive: `{CardTypes:[creature], IsToken:false, Controller:You}`, no `ExcludeSelf`.
  **Sibling check PASSES:** Bane, Lord of Darkness ("another nontoken creature", NO "or another")
  → `isDisjunction=false` → gold correctly keeps `ExcludeSelf:true`. Anax + Bane are the only two
  nontoken-creature-dies fixtures; the guard flips neither incorrectly. (CR 109.5 — "another"
  excludes the source.)

- **J03 — Runed Stalactite** — PASS. `AttachedModifyPTAndEveryCreatureTypeRule` now cites
  **CR 702.73a** (Changeling CDA, verbatim) instead of the wrong "CR 702.6 — Changeling". Verified
  in rules-structure.json: **702.6 = Equip** (old cite was wrong); **702.73a = Changeling**
  ("This object is every creature type"). No residual 702.6-for-Changeling in the file.

- **J04 — Wingrattle Scarecrow** — PASS. `AsLongAsControlColorCreatureSelfKeywordGrantRule` changed
  **611.2 → 611.3a**. Verified: 611.2 = continuous effect from *resolution* (wrong for a static
  grant); **611.3 = continuous effect from the static ability of an object**; **611.3a** = "isn't
  'locked in'; applies at any given moment." Correct axis. No residual non-.3 611.2. (Bonus: its
  702.79a Persist cite also verified = Persist.)

- **J16 — Rune of Flight** — PASS. Both `ObjectHasCardTypeCondition.cs` and
  `AsLongAsEnchantedTypeKeywordGrantRule.cs` replaced the **fabricated "CR 611.2c (verbatim)"** quote
  with the real **CR 611.3a** text: "A continuous effect generated by a static ability isn't
  'locked in'; it applies at any given moment to whatever its text indicates." Byte-for-byte matches
  rules-structure.json's first 611.3 subrule. `grep` confirms NO residual 611.2c in either file.

- **J17 — Cloudshredder Sliver / Dalakos** — PASS. `_reservedNonSubtypeWords` expanded from
  {Other, Nontoken, Attacking} to a closed 36-word vocabulary
  (colors + supertypes + co-card-type qualifiers + permanent-state adjectives + structural),
  including **"Equipped"** → Dalakos ("Equipped creatures you control have flying and haste") now
  declines (returns null) instead of fabricating `Subtypes:["Equipped"]`.
  **CRITICAL SWEEP:** intersected all 36 declined words against the **317 real creature subtypes**
  in `type-ontology.json` (creature subtypePool) → **ZERO collisions**. No legitimate creature
  subtype is accidentally declined. "Sliver" is NOT in the decline set and IS a real subtype →
  Cloudshredder still parses to `Subtypes:["Sliver"]`. Ordinal (case-sensitive) compare aligns with
  the regex `[A-Z][a-z]+` Titlecase capture, so no case-escape.

## FAIL verdicts

None.

## Process notes

- The five remediated files show as "new file" against base `c4f2a3e6` because that SHA predates
  the whole batch; the CURRENT HEAD (`b160e112`) content was judged directly.
- The J17 decline-set soundness rests on a closed-vocabulary argument: any real creature subtype is
  absent from the set and passes through. The empirical intersection against all 317 subtypes
  confirms the argument holds for the concrete list as written.

**RESULT: ALL PASS — PROCEED.**
