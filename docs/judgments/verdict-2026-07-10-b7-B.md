# MAST judge — batch 7 shard B verdict

**Date:** 2026-07-10
**Scope:** 5 families (5 fixtures + new AST field-adds / whitelist judgment calls)
**Result:** PASS

## Summary

- PASS: 6
- FAIL: 0

## FAIL verdicts

None.

## PASS verdicts

- `tests/.../MirrorBox.json` (L13) — PASS. All 3 abilities structured: `legendRuleSuppression` scoped to Each permanent you control (CR 704.5j, permanent pseudo-type CR 110.4a); legendary anthem `modifyPT` +1/+1 (CR 205.4a supertype, layer 7c CR 613.4c); same-name-count `modifyPT` whose modifiers are `count` of `{creature, SharesNameWith:{It}, Controller:You, ExcludeSelf:true}` — "for each other creature you control with the same name as that creature" (CR 201.2 same-name comparison; CR 611.3 static continuous effect). No unparsed / free-text.
- `libs/magic-ast/AST/References/ObjectFilterRelations.cs#SharesNameWith` — PASS. The new `SharesNameWith` relational axis is added to BOTH soundness floors — `UndecidedAxis` (line 519, used by `Intersects`) and `SupUndecidedAxis` (line 857, used by `Subsumes`) — so both stay sound; a missing floor entry would have been a soundness FAIL. Parallel to `SharesCreatureTypeWith`. The field is nullable + `JsonIgnore(WhenWritingNull)`, so existing fixtures serialize byte-identically. ObjectFilter relational axes are not PortWalk discriminators (sibling `SharesCreatureTypeWith`/`SharesColorWith` appear in no projection config), so no projection decision is required.
- `tests/.../MyrelShieldOfArgive.json` (L03) — PASS. Two `static` prohibitions — `cantBeCast` over `{spell, Opponent}` (CR 601.3a) and `cantActivateAbilities` over the `{artifact, creature, enchantment}` disjunction `{Controller:Opponent}` (CR 602.5 "a player can't begin to activate an ability that's prohibited") — sharing one whitelisted `OtherCondition{"During your turn"}`; plus the attack-triggered `createToken` with `Count = number of Soldiers you control`. Full card structured, no `unparsed`.
- `tests/.../GimliOfTheGlitteringCaves.json` (L12) — PASS. Double strike (`combatDamageTiming` Both, CR 702.4); new enters-trigger `{creature, Legendary, Controller:You, ExcludeSelf:true}` → `putCounters` +1/+1 on self (CR 603.6a enters-trigger, CR 603.2, CR 205.4 Legendary supertype; ExcludeSelf per "another"); combat-damage-to-player trigger → Treasure `createToken`. Whitelisted `AbilityText` reminder is byte-consistent with the AncientCopperDragon precedent (same sink/reason) and is the standard verbatim Treasure ability text.
- `tests/.../SilverquillSilencer.json` (L17) — PASS. As-enters choice decomposed correctly as `When:asThisEnters` + plain `chooseCardName` (timing NOT baked into the effect) with a new additive nonland `Filter{CardTypes:["card"], ExcludedCardTypes:["land"]}`; the consumer trigger uses the structured `ChosenCharacteristic:CardName` (CR 607 linked ability — a structured variable reference, NOT the free-text "of the chosen name" shortcut). Composite "they lose 3 life and you draw a card" splits into `[loseLife(ThatPlayer,3), drawCards(You,1)]` (CR 119.3 / 121.1). CR 614.12's own worked example is literally "As this creature enters, choose a color", so the citation is exact. Declaration of Naught (K23, unscoped) is byte-identical (empty diff, no `Filter`).
- `tests/.../PrimevalProtector.json` (L11) — PASS. `costReduction` Amount 1, `PerObject{creature, Controller:Opponent}` — "for each creature your opponents control" (CR 118.7 cost reduction, correctly NOT the alternative-cost rule 118.9); enters-trigger `putCounters` over `Each {creature, You, ExcludeSelf:true}`. Both shared-rule branches are proven disjoint from siblings: the `CostReductionForEachRule` branch keys on the exact filter phrase, and the `PutCountersTriggeredRule` bare "each other creature you control" pattern cannot collide with the subtype-required each-other pattern (no word between "other" and "creature").

## Glossary gaps

None surfaced.

## Process notes

- **CR 115.3 mis-citation (systemic, non-halting).** Myrel's `OpponentsCantCastOrActivateDuringYourTurnRule` doc-comment cites "OR semantics per CR 115.3" for the artifact/creature/enchantment type-disjunction. CR 115.3 is the targeting-multiplicity rule ("the same target can't be chosen multiple times…") and does not ground type-list OR-semantics. This is NOT a Myrel-invented error — it is a pre-existing codebase-wide convention (`ReturnUpToOneTargetTypeDisjunctionToHandEffectRule`, `ReturnArtifactOrEnchantmentFromGraveyardSpellRule`, `MillThenOptionalReturnFromGraveyardToHandRule` all cite it at base). The AST modeling (a `CardTypes` list read disjunctively) is faithful, so this does not corrupt gold data. Recommend a follow-up doc-comment cleanup across the family (a type list is grounded in the type definitions, not the targeting rule); not batch-halting since it is inherited and the primary load-bearing citations (601.3a / 602.5) are sound.
- **L13 supporting-citation imprecision (non-halting).** The `SharesNameWith` doc-comment's ancillary asides — CR 109.2 glossed as "a pronoun to a previously-mentioned object" (109.2 is actually about object-description defaults) and CR 201.4 as "the absolute Name axis" (201.4 is about choosing a card name; 201.5 would be more apt) — are imprecise. Both numbers exist and neither contradicts the new field's modeling; the primary, load-bearing citation for `SharesNameWith` (CR 201.2 same-name) is correct.
- No branch in this shard adds a PortWalk-relevant discriminator (all effect/trigger types are reused; the new `ObjectFilter.SharesNameWith` / `ChooseCardNameEffect.Filter` are filter axes/sub-fields, not PortGraph cases), and none touch the projection config — so no projection-decision verdict is required.
