# MAST judge — batch 7 shard C verdict

**Date:** 2026-07-10
**Scope:** 10 targets (5 fixtures, 5 AST/parser rules) — reuse-dominated cost-reduction / doubling / tapped / chosen-color families
**Result:** PASS (10 PASS / 0 FAIL)

## Summary

- PASS: 10
- FAIL: 0

All five branches reuse existing discriminators (no new effect/cost type, trigger event, or restriction), so no PortWalk projection decision is triggered — the initiative-03 ratchet enforces presence only on new discriminators, and none appear here. Verified `additionalTime`, `domain` quantity, `costReduction`, `tap`/`Each`, `ChosenCharacteristicKind.Color`, `chooseColor`, `AddManaEffect.OfChosenColor` all pre-exist at base `d480b48b`.

Every fixture's `Input.OracleText` was cross-checked byte-for-byte against `oracle-cards.json` — all five match verbatim (including Yarok's modern "a permanent entering" templating and Stone Calendar's "Spells you cast cost {1} less").

## FAIL verdicts

None.

## PASS verdicts

- `YarokTheDesecrated.json` — PASS. Deathtouch/lifelink keywords + a `replacement` over `abilityTrigger` (AffectedObjects = permanent+You, CausedByEntering = permanent, `additionalTime`) — faithful CR 603.2d any-permanent trigger doubling. No unparsed.
- `PermanentTriggerDoublingRule.cs` — PASS. CR 603.2d exists and matches. Regex anchored to "a permanent entering"; LandTriggerDoublingRule (K06) anchors "a land entering" — both `^…$`, mutually exclusive, no collision. Reuses the accepted `additionalTime` modifier shared by Land/Panharmonicon/Drivnod doublers.
- `Stratadon.json` — PASS. `costReduction` Amount = bare `DomainQuantity` (1×domain identity) + `AbilityWord: "Domain"` + trample. Faithful to "Domain — This spell costs {1} less … for each basic land type among lands you control."
- `DomainSpellCostReductionRule.cs` — PASS. Cites CR 118.7 + 305.6 (correctly NOT the brief's erroneous 700.6, which is historic) + 207.2c domain ability word — all exist and match. Anchored own-file rule; no overlap with the shared CostReductionForEachRule.
- `StoneCalendar.json` — PASS. `costReduction` literal 1 + AffectedObjects = spell+You bare filter, faithful to the unrestricted "Spells you cast cost {1} less to cast."
- `UnrestrictedSpellCostReductionRule.cs` — PASS. CR 118.7/118.7a exist/match. `^Spells\s+you\s+cast…$` anchor (no leading qualifier noun, case-sensitive) cannot capture any qualified "X spells you cast cost {N} less" sibling — every qualified card starts with a qualifier noun.
- `FrozenAether.json` — PASS. `When: asObjectEnters` + `tap` Each{artifact,creature,land; Opponent} — faithful three-type disjunction, CR 614.1d / 614.12.
- `EntersTappedRule.cs#Arm5` — PASS. Arm 5 anchored to the exact "Artifacts, creatures, and lands your opponents control enter tapped." surface; Arm 3 (bare "Creatures…") and Arm 4 ("Creatures and nonbasic lands…") both anchor on "Creatures", so Arm 5 cannot steal them and vice versa. Correctly also covers the fixtureless Kismet (identical text).
- `HeraldicBanner.json` — PASS. Producer `chooseColor` (`When: asThisEnters`, timing decomposed — NOT a `chooseColorOnEntry` bake-in) + anthem `modifyPT` using STRUCTURED `ChosenCharacteristic: Color` (not the free-text "of the chosen color" anti-pattern) + mana ability `OfChosenColor`. CR 611.3 continuous-from-static + 607 linked ability. No unparsed.
- `ChosenColorAnthemModifyPTRule.cs` — PASS. CR 611.3/607 exist and match. Consumer references the chosen color via the structured `ChosenCharacteristicKind.Color` reference — the established, already-accepted mechanism (direct sibling of `ChosenTypeAnthemModifyPTRule`), not free text. Distinct "color" vs "type" surface → no sibling regex overlap.

## Glossary gaps

None. Domain (CR 207.2c ability word), deathtouch (702.2), lifelink (702.15), trample all covered by rules data; "domain" is listed in the CR 207.2c ability-word enumeration.

## Process notes

- **Projection (initiative 03):** none of the 5 branches touches an AST node / enum / projection file — each diff is exactly (new parser rule .cs + fixture .json). All discriminators are pre-existing, so no `PortGraph`/`PortWalkProjection` entry nor `known-coarse-projections.json` entry is required, and none was expected. No projection verdict emitted (the condition — a new discriminator — is not met).
- **Sibling sweep (edge-diff offline):** performed by static regex-anchor analysis. All three at-risk overlaps (Yarok vs K06 land-only; Stone Calendar vs qualified spell-reduction; Frozen Aether Arm 5 vs Arms 3/4) are ruled out by mutually-exclusive `^…$` surface anchors.
- **Lifelink/deathtouch modeling asymmetry** (lifelink → bespoke `lifelink` effectType vs deathtouch → `keywordAbility`) is pre-existing codebase convention, not introduced here, and is rules-accurate either way — not a FAIL.
