# MAST judge — batch verdict (2026-07-10 batch-1, SHARD A)

**Date:** 2026-07-10
**Scope:** 4 worker branches (SHARD A — new effect-node families). Base sha `2e1bff25`.
**Result:** PASS (4/4)

## Summary

- PASS: 4
- FAIL: 0

## FAIL verdicts

(none)

## PASS verdicts

- `mast-tdd/2026-07-10-morph-reveal-cost` — PASS. New `RevealCost` (`reveal`) records the DISTINCT
  "turn face up" morph cost ("Reveal a red card in your hand") with Filter/Quantity/Zone, mirroring
  `DiscardCost`/`ExileCost`; the fixed {3} face-down cast cost is correctly NOT recorded (CR 702.37a,
  verified present; the reminder mana-empty concept ties to CR 106.4). Both Morph combinator variants
  are `.Try()`-guarded and anchored, so the reveal variant cannot cannibalise the mana variant.
  `TurnedFaceUp` trigger uses the established codebase filter convention (matches StormwingDragon).
  Coarse projection `reveal` (costType) sensible: an information-disclosure cost, no zone change /
  no resource loss, so it originates no cross-card resource a flow rule consumes (revealCards precedent).

- `mast-tdd/2026-07-10-equipped-gets-pt-and-goaded` — PASS. `GoadedEffect` (`goaded`) records the goad
  designation as a continuous fact on `Target`, doc-comment quoting CR 701.15b verbatim (matches the
  rules data exactly) and explicitly deferring the attacks-each-combat/attacks-elsewhere requirements to
  the engine — described-not-executed. `AttachThisToTargetEffectRule` is anchored `^…$`; the `$` anchor
  makes filter-drop impossible (Luxior's "up to one target" correctly fails to match rather than
  mislabelling), and it correctly upgrades the standalone activated re-attach abilities (Horned Helm,
  Felidar Umbra). `AttachedModifyPTAndGoadedRule` is anchored and precisely captures only the
  "gets +N/+M and is goaded" shape — it correctly models the whole "Impetus" cycle (Psychic/Shiny/
  Parasitic/… + Mark of the Rani) as `modifyPT`+`goaded`, and correctly EXCLUDES the compound siblings
  it must not partial-match (Ghoulish Impetus "has deathtouch, and is goaded", Predatory Impetus "must
  be blocked if able, and is goaded", bare "is goaded" Sound of Drums). Because `goaded` is brand-new,
  it can only improve these siblings, never regress them. Coarse projection `goaded` sensible (sibling
  of already-coarse mustAttack/cantAttack combat-restriction family; MAST does not execute combat).

- `mast-tdd/2026-07-10-dont-lose-unspent-mana` — PASS (heavy scrutiny cleared on all three axes).
  (a) `RetainUnspentManaEffect` (`retainUnspentMana`) is correct and described-not-executed: a
  fieldless-but-for-`Color` continuous fact recording that the named color's mana is exempt from the
  step/phase mana-emptying action, leaving the pool bookkeeping to the engine; CR 500.4 and CR 106.4
  doc-comment quotes verified verbatim against the rules data; cleanly distinguished from the
  turn-scoped `ManaPersistsUntilEndOfTurnEffect`. The static rule is anchored `^…$`.
  (b) The bespoke `TryParseDiscardDrawThenEscalatingResolutionCountEffects` matcher is FULLY ANCHORED
  `^…$` over Ashling's ENTIRE escalating sequence (discard→draw→"second time … deals N to each opponent
  and each creature they control"→"third time, add {mana}"). Corpus cross-check: many cards share the
  "discard a card, then draw a card" fragment and many share "second time this ability has resolved
  this turn", but ONLY Ashling appears in both — and the regex demands the full multi-sentence template
  on top of that — so it cannot regress the ~5 sibling `, then` composites or any other magecraft card.
  A one-card matcher that is anchored is acceptable per the dispatch criterion (the FAIL condition was
  an *unanchored* one-card overfit).
  (c) The `(AshlingFlameDancer, OtherCondition)` carve-out is genuinely irreducible-for-now: no
  structured `NthTime…Resolved` condition node exists yet, it is tagged `debt` under the PB-7
  structured-condition bucket, and the peer precedents are real — `LCI/NissaResurgentAnimist`
  ("second time this ability has resolved") and `SephirothFabledSOLDIER…` ("fourth time this ability has
  resolved") are both already whitelisted `OtherCondition` in the same family. This is the doctrine's
  sanctioned "route to design", not a lazy inline. Coarse projection `retainUnspentMana` sensible
  (sibling of coarse manaPersists; no flow rule reads mana-retention grants yet).

- `mast-tdd/2026-07-10-defiler-additional-cost-life` — PASS. `GrantAdditionalCostEffect`
  (`grantAdditionalCost`, carrying an optional `AdditionalCost` + an `AffectedSpells` filter over a
  class of OTHER spells) and `AdditionalCostPaidCondition` (`additionalCostPaid`, a fieldless marker for
  "if you paid life this way") are the right shapes and siblings of `GrantAlternativeCostEffect` /
  `KeywordCostPaidCondition`; both doc-comments quote CR 601.2f verbatim (matches the rules data:
  "…plus all additional costs and cost increases, and minus all cost reductions"). The new optional
  `CostReductionEffect.ManaSymbols` cleanly encodes a colored ({R}) reduction (Amount=0 generic +
  ManaSymbols=[{R}]), mirroring `CostIncreaseEffect.ManaSymbols`, and absorbs the "reduces only red
  mana" clarifier structurally. The `SpellCastConditionRule` word-list edit is purely additive —
  "permanent"/"planeswalker"/"land"/"battle" are appended AFTER the untouched five words with no logic
  change — so every existing "whenever you cast a [type] spell" sibling parses identically; the change
  only ADDS type recognition (never drops a filter), exactly as required. Coarse projection
  `grantAdditionalCost` sensible (sibling of grantAlternativeCost; no flow rule reads additional-cost
  grants yet).

## Glossary gaps

(none surfaced this shard.)

## Process notes

- The corpus edge-diff gate is offline this batch; sibling-mislabel defense was performed manually by
  grepping `oracle-cards.json`. Two broad-ish matchers were the focus: (i) the goad P/T-anthem rule —
  verified precise against the full Impetus cycle; (ii) the `AttachThisToTargetEffectRule` — verified
  its `$` anchor makes filter-drops structurally impossible (unmatched clauses simply fall through).
- Branch `dont-lose-unspent-mana` edits a parser body (`TriggeredAbilityParser`) — parser correctness
  is NUnit's job and out of judge scope; the judge-relevant axis (regression-safety of the anchored
  bespoke matcher) is clean. Flagging the parser-body edit only as a code-review note for the human.
- All four fixtures scanned: zero `unparsed` occurrences. All four coarse-projection ratchet entries
  present with plausible, sibling-anchored justifications.

**PROCEED.**
